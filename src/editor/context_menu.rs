use crate::editor::core::MarkdownEditor;

impl MarkdownEditor {
    // Context menu functionality is primarily handled by the ContextMenu struct
    // from the context_menu module. This file is kept for any additional
    // context menu related functionality that might be specific to the editor.
    
    // Currently no additional context menu methods needed here
    // as they are handled by the dedicated ContextMenu struct
}
