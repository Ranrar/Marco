// This file is generated by build.rs
use marco::logic::core::inline::parser::parse_phrases;
use marco::logic::renderer::html_render::HtmlRenderer;
#[cfg(test)]
mod render_spec_generated {
    use super::*;
    #[test]
    fn render_example_1() {
        let ast = parse_phrases(r#"	foo	baz		bim
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>foo	baz		bim
</code></pre>
"###.trim(), "Render mismatch for example 1");
    }
    #[test]
    fn render_example_2() {
        let ast = parse_phrases(r#"  	foo	baz		bim
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>foo	baz		bim
</code></pre>
"###.trim(), "Render mismatch for example 2");
    }
    #[test]
    fn render_example_3() {
        let ast = parse_phrases(r#"    a	a
    ὐ	a
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>a	a
ὐ	a
</code></pre>
"###.trim(), "Render mismatch for example 3");
    }
    #[test]
    fn render_example_4() {
        let ast = parse_phrases(r#"  - foo

	bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>foo</p>
<p>bar</p>
</li>
</ul>
"###.trim(), "Render mismatch for example 4");
    }
    #[test]
    fn render_example_5() {
        let ast = parse_phrases(r#"- foo

		bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>foo</p>
<pre><code>  bar
</code></pre>
</li>
</ul>
"###.trim(), "Render mismatch for example 5");
    }
    #[test]
    fn render_example_6() {
        let ast = parse_phrases(r#">		foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<pre><code>  foo
</code></pre>
</blockquote>
"###.trim(), "Render mismatch for example 6");
    }
    #[test]
    fn render_example_7() {
        let ast = parse_phrases(r#"-		foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<pre><code>  foo
</code></pre>
</li>
</ul>
"###.trim(), "Render mismatch for example 7");
    }
    #[test]
    fn render_example_8() {
        let ast = parse_phrases(r#"    foo
	bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>foo
bar
</code></pre>
"###.trim(), "Render mismatch for example 8");
    }
    #[test]
    fn render_example_9() {
        let ast = parse_phrases(r#" - foo
   - bar
	 - baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo
<ul>
<li>bar
<ul>
<li>baz</li>
</ul>
</li>
</ul>
</li>
</ul>
"###.trim(), "Render mismatch for example 9");
    }
    #[test]
    fn render_example_10() {
        let ast = parse_phrases(r#"#	Foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h1>Foo</h1>
"###.trim(), "Render mismatch for example 10");
    }
    #[test]
    fn render_example_11() {
        let ast = parse_phrases(r#"*	*	*	
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<hr />
"###.trim(), "Render mismatch for example 11");
    }
    #[test]
    fn render_example_12() {
        let ast = parse_phrases(r#"\!\\"\#\$\%\&\'\(\)\*\+\,\-\.\/\:\;\<\=\>\?\@\[\\\]\^\_\`\{\|\}\~
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>!&quot;#$%&amp;'()*+,-./:;&lt;=&gt;?@[\]^_`{|}~</p>
"###.trim(), "Render mismatch for example 12");
    }
    #[test]
    fn render_example_13() {
        let ast = parse_phrases(r#"\	\A\a\ \3\φ\«
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>\	\A\a\ \3\φ\«</p>
"###.trim(), "Render mismatch for example 13");
    }
    #[test]
    fn render_example_14() {
        let ast = parse_phrases(r#"\*not emphasized*
\<br/> not a tag
\[not a link](/foo)
\`not code`
1\. not a list
\* not a list
\# not a heading
\[foo]: /url \"not a reference\"
\&ouml; not a character entity
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*not emphasized*
&lt;br/&gt; not a tag
[not a link](/foo)
`not code`
1. not a list
* not a list
# not a heading
[foo]: /url &quot;not a reference&quot;
&amp;ouml; not a character entity</p>
"###.trim(), "Render mismatch for example 14");
    }
    #[test]
    fn render_example_15() {
        let ast = parse_phrases(r#"\\*emphasis*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>\<em>emphasis</em></p>
"###.trim(), "Render mismatch for example 15");
    }
    #[test]
    fn render_example_16() {
        let ast = parse_phrases(r#"foo\
bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo<br />
bar</p>
"###.trim(), "Render mismatch for example 16");
    }
    #[test]
    fn render_example_17() {
        let ast = parse_phrases(r#"`` \[\` ``
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>\[\`</code></p>
"###.trim(), "Render mismatch for example 17");
    }
    #[test]
    fn render_example_18() {
        let ast = parse_phrases(r#"    \[\]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>\[\]
</code></pre>
"###.trim(), "Render mismatch for example 18");
    }
    #[test]
    fn render_example_19() {
        let ast = parse_phrases(r#"~~~
\[\]
~~~
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>\[\]
</code></pre>
"###.trim(), "Render mismatch for example 19");
    }
    #[test]
    fn render_example_20() {
        let ast = parse_phrases(r#"<https://example.com?find=\*>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="https://example.com?find=%5C*">https://example.com?find=\*</a></p>
"###.trim(), "Render mismatch for example 20");
    }
    #[test]
    fn render_example_21() {
        let ast = parse_phrases(r#"<a href=\"/bar\/)\">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<a href="/bar\/)">
"###.trim(), "Render mismatch for example 21");
    }
    #[test]
    fn render_example_22() {
        let ast = parse_phrases(r#"[foo](/bar\* \"ti\*tle\")
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/bar*" title="ti*tle">foo</a></p>
"###.trim(), "Render mismatch for example 22");
    }
    #[test]
    fn render_example_23() {
        let ast = parse_phrases(r#"[foo]

[foo]: /bar\* \"ti\*tle\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/bar*" title="ti*tle">foo</a></p>
"###.trim(), "Render mismatch for example 23");
    }
    #[test]
    fn render_example_24() {
        let ast = parse_phrases(r#"``` foo\+bar
foo
```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code class="language-foo+bar">foo
</code></pre>
"###.trim(), "Render mismatch for example 24");
    }
    #[test]
    fn render_example_25() {
        let ast = parse_phrases(r#"&nbsp; &amp; &copy; &AElig; &Dcaron;
&frac34; &HilbertSpace; &DifferentialD;
&ClockwiseContourIntegral; &ngE;
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>  &amp; © Æ Ď
¾ ℋ ⅆ
∲ ≧̸</p>
"###.trim(), "Render mismatch for example 25");
    }
    #[test]
    fn render_example_26() {
        let ast = parse_phrases(r#"&#35; &#1234; &#992; &#0;
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p># Ӓ Ϡ �</p>
"###.trim(), "Render mismatch for example 26");
    }
    #[test]
    fn render_example_27() {
        let ast = parse_phrases(r#"&#X22; &#XD06; &#xcab;
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&quot; ആ ಫ</p>
"###.trim(), "Render mismatch for example 27");
    }
    #[test]
    fn render_example_28() {
        let ast = parse_phrases(r#"&nbsp &x; &#; &#x;
&#87654321;
&#abcdef0;
&ThisIsNotDefined; &hi?;
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&amp;nbsp &amp;x; &amp;#; &amp;#x;
&amp;#87654321;
&amp;#abcdef0;
&amp;ThisIsNotDefined; &amp;hi?;</p>
"###.trim(), "Render mismatch for example 28");
    }
    #[test]
    fn render_example_29() {
        let ast = parse_phrases(r#"&copy
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&amp;copy</p>
"###.trim(), "Render mismatch for example 29");
    }
    #[test]
    fn render_example_30() {
        let ast = parse_phrases(r#"&MadeUpEntity;
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&amp;MadeUpEntity;</p>
"###.trim(), "Render mismatch for example 30");
    }
    #[test]
    fn render_example_31() {
        let ast = parse_phrases(r#"<a href=\"&ouml;&ouml;.html\">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<a href="&ouml;&ouml;.html">
"###.trim(), "Render mismatch for example 31");
    }
    #[test]
    fn render_example_32() {
        let ast = parse_phrases(r#"[foo](/f&ouml;&ouml; \"f&ouml;&ouml;\")
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/f%C3%B6%C3%B6" title="föö">foo</a></p>
"###.trim(), "Render mismatch for example 32");
    }
    #[test]
    fn render_example_33() {
        let ast = parse_phrases(r#"[foo]

[foo]: /f&ouml;&ouml; \"f&ouml;&ouml;\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/f%C3%B6%C3%B6" title="föö">foo</a></p>
"###.trim(), "Render mismatch for example 33");
    }
    #[test]
    fn render_example_34() {
        let ast = parse_phrases(r#"``` f&ouml;&ouml;
foo
```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code class="language-föö">foo
</code></pre>
"###.trim(), "Render mismatch for example 34");
    }
    #[test]
    fn render_example_35() {
        let ast = parse_phrases(r#"`f&ouml;&ouml;`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>f&amp;ouml;&amp;ouml;</code></p>
"###.trim(), "Render mismatch for example 35");
    }
    #[test]
    fn render_example_36() {
        let ast = parse_phrases(r#"    f&ouml;f&ouml;
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>f&amp;ouml;f&amp;ouml;
</code></pre>
"###.trim(), "Render mismatch for example 36");
    }
    #[test]
    fn render_example_37() {
        let ast = parse_phrases(r#"&#42;foo&#42;
*foo*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*foo*
<em>foo</em></p>
"###.trim(), "Render mismatch for example 37");
    }
    #[test]
    fn render_example_38() {
        let ast = parse_phrases(r#"&#42; foo

* foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>* foo</p>
<ul>
<li>foo</li>
</ul>
"###.trim(), "Render mismatch for example 38");
    }
    #[test]
    fn render_example_39() {
        let ast = parse_phrases(r#"foo&#10;&#10;bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo

bar</p>
"###.trim(), "Render mismatch for example 39");
    }
    #[test]
    fn render_example_40() {
        let ast = parse_phrases(r#"&#9;foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>	foo</p>
"###.trim(), "Render mismatch for example 40");
    }
    #[test]
    fn render_example_41() {
        let ast = parse_phrases(r#"[a](url &quot;tit&quot;)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[a](url &quot;tit&quot;)</p>
"###.trim(), "Render mismatch for example 41");
    }
    #[test]
    fn render_example_42() {
        let ast = parse_phrases(r#"- `one
- two`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>`one</li>
<li>two`</li>
</ul>
"###.trim(), "Render mismatch for example 42");
    }
    #[test]
    fn render_example_43() {
        let ast = parse_phrases(r#"***
---
___
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<hr />
<hr />
<hr />
"###.trim(), "Render mismatch for example 43");
    }
    #[test]
    fn render_example_44() {
        let ast = parse_phrases(r#"+++
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>+++</p>
"###.trim(), "Render mismatch for example 44");
    }
    #[test]
    fn render_example_45() {
        let ast = parse_phrases(r#"===
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>===</p>
"###.trim(), "Render mismatch for example 45");
    }
    #[test]
    fn render_example_46() {
        let ast = parse_phrases(r#"--
**
__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>--
**
__</p>
"###.trim(), "Render mismatch for example 46");
    }
    #[test]
    fn render_example_47() {
        let ast = parse_phrases(r#" ***
  ***
   ***
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<hr />
<hr />
<hr />
"###.trim(), "Render mismatch for example 47");
    }
    #[test]
    fn render_example_48() {
        let ast = parse_phrases(r#"    ***
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>***
</code></pre>
"###.trim(), "Render mismatch for example 48");
    }
    #[test]
    fn render_example_49() {
        let ast = parse_phrases(r#"Foo
    ***
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo
***</p>
"###.trim(), "Render mismatch for example 49");
    }
    #[test]
    fn render_example_50() {
        let ast = parse_phrases(r#"_____________________________________
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<hr />
"###.trim(), "Render mismatch for example 50");
    }
    #[test]
    fn render_example_51() {
        let ast = parse_phrases(r#" - - -
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<hr />
"###.trim(), "Render mismatch for example 51");
    }
    #[test]
    fn render_example_52() {
        let ast = parse_phrases(r#" **  * ** * ** * **
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<hr />
"###.trim(), "Render mismatch for example 52");
    }
    #[test]
    fn render_example_53() {
        let ast = parse_phrases(r#"-     -      -      -
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<hr />
"###.trim(), "Render mismatch for example 53");
    }
    #[test]
    fn render_example_54() {
        let ast = parse_phrases(r#"- - - -    
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<hr />
"###.trim(), "Render mismatch for example 54");
    }
    #[test]
    fn render_example_55() {
        let ast = parse_phrases(r#"_ _ _ _ a

a------

---a---
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>_ _ _ _ a</p>
<p>a------</p>
<p>---a---</p>
"###.trim(), "Render mismatch for example 55");
    }
    #[test]
    fn render_example_56() {
        let ast = parse_phrases(r#" *-*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>-</em></p>
"###.trim(), "Render mismatch for example 56");
    }
    #[test]
    fn render_example_57() {
        let ast = parse_phrases(r#"- foo
***
- bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo</li>
</ul>
<hr />
<ul>
<li>bar</li>
</ul>
"###.trim(), "Render mismatch for example 57");
    }
    #[test]
    fn render_example_58() {
        let ast = parse_phrases(r#"Foo
***
bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo</p>
<hr />
<p>bar</p>
"###.trim(), "Render mismatch for example 58");
    }
    #[test]
    fn render_example_59() {
        let ast = parse_phrases(r#"Foo
---
bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h2>Foo</h2>
<p>bar</p>
"###.trim(), "Render mismatch for example 59");
    }
    #[test]
    fn render_example_60() {
        let ast = parse_phrases(r#"* Foo
* * *
* Bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>Foo</li>
</ul>
<hr />
<ul>
<li>Bar</li>
</ul>
"###.trim(), "Render mismatch for example 60");
    }
    #[test]
    fn render_example_61() {
        let ast = parse_phrases(r#"- Foo
- * * *
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>Foo</li>
<li>
<hr />
</li>
</ul>
"###.trim(), "Render mismatch for example 61");
    }
    #[test]
    fn render_example_62() {
        let ast = parse_phrases(r#"# foo
## foo
### foo
#### foo
##### foo
###### foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h1>foo</h1>
<h2>foo</h2>
<h3>foo</h3>
<h4>foo</h4>
<h5>foo</h5>
<h6>foo</h6>
"###.trim(), "Render mismatch for example 62");
    }
    #[test]
    fn render_example_63() {
        let ast = parse_phrases(r#"####### foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>####### foo</p>
"###.trim(), "Render mismatch for example 63");
    }
    #[test]
    fn render_example_64() {
        let ast = parse_phrases(r#"#5 bolt

#hashtag
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>#5 bolt</p>
<p>#hashtag</p>
"###.trim(), "Render mismatch for example 64");
    }
    #[test]
    fn render_example_65() {
        let ast = parse_phrases(r#"\## foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>## foo</p>
"###.trim(), "Render mismatch for example 65");
    }
    #[test]
    fn render_example_66() {
        let ast = parse_phrases(r#"# foo *bar* \*baz\*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h1>foo <em>bar</em> *baz*</h1>
"###.trim(), "Render mismatch for example 66");
    }
    #[test]
    fn render_example_67() {
        let ast = parse_phrases(r#"#                  foo                     
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h1>foo</h1>
"###.trim(), "Render mismatch for example 67");
    }
    #[test]
    fn render_example_68() {
        let ast = parse_phrases(r#" ### foo
  ## foo
   # foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h3>foo</h3>
<h2>foo</h2>
<h1>foo</h1>
"###.trim(), "Render mismatch for example 68");
    }
    #[test]
    fn render_example_69() {
        let ast = parse_phrases(r#"    # foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code># foo
</code></pre>
"###.trim(), "Render mismatch for example 69");
    }
    #[test]
    fn render_example_70() {
        let ast = parse_phrases(r#"foo
    # bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo
# bar</p>
"###.trim(), "Render mismatch for example 70");
    }
    #[test]
    fn render_example_71() {
        let ast = parse_phrases(r#"## foo ##
  ###   bar    ###
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h2>foo</h2>
<h3>bar</h3>
"###.trim(), "Render mismatch for example 71");
    }
    #[test]
    fn render_example_72() {
        let ast = parse_phrases(r#"# foo ##################################
##### foo ##
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h1>foo</h1>
<h5>foo</h5>
"###.trim(), "Render mismatch for example 72");
    }
    #[test]
    fn render_example_73() {
        let ast = parse_phrases(r#"### foo ###     
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h3>foo</h3>
"###.trim(), "Render mismatch for example 73");
    }
    #[test]
    fn render_example_74() {
        let ast = parse_phrases(r#"### foo ### b
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h3>foo ### b</h3>
"###.trim(), "Render mismatch for example 74");
    }
    #[test]
    fn render_example_75() {
        let ast = parse_phrases(r#"# foo#
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h1>foo#</h1>
"###.trim(), "Render mismatch for example 75");
    }
    #[test]
    fn render_example_76() {
        let ast = parse_phrases(r#"### foo \###
## foo #\##
# foo \#
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h3>foo ###</h3>
<h2>foo ###</h2>
<h1>foo #</h1>
"###.trim(), "Render mismatch for example 76");
    }
    #[test]
    fn render_example_77() {
        let ast = parse_phrases(r#"****
## foo
****
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<hr />
<h2>foo</h2>
<hr />
"###.trim(), "Render mismatch for example 77");
    }
    #[test]
    fn render_example_78() {
        let ast = parse_phrases(r#"Foo bar
# baz
Bar foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo bar</p>
<h1>baz</h1>
<p>Bar foo</p>
"###.trim(), "Render mismatch for example 78");
    }
    #[test]
    fn render_example_79() {
        let ast = parse_phrases(r#"## 
#
### ###
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h2></h2>
<h1></h1>
<h3></h3>
"###.trim(), "Render mismatch for example 79");
    }
    #[test]
    fn render_example_80() {
        let ast = parse_phrases(r#"Foo *bar*
=========

Foo *bar*
---------
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h1>Foo <em>bar</em></h1>
<h2>Foo <em>bar</em></h2>
"###.trim(), "Render mismatch for example 80");
    }
    #[test]
    fn render_example_81() {
        let ast = parse_phrases(r#"Foo *bar
baz*
====
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h1>Foo <em>bar
baz</em></h1>
"###.trim(), "Render mismatch for example 81");
    }
    #[test]
    fn render_example_82() {
        let ast = parse_phrases(r#"  Foo *bar
baz*	
====
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h1>Foo <em>bar
baz</em></h1>
"###.trim(), "Render mismatch for example 82");
    }
    #[test]
    fn render_example_83() {
        let ast = parse_phrases(r#"Foo
-------------------------

Foo
=
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h2>Foo</h2>
<h1>Foo</h1>
"###.trim(), "Render mismatch for example 83");
    }
    #[test]
    fn render_example_84() {
        let ast = parse_phrases(r#"   Foo
---

  Foo
-----

  Foo
  ===
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h2>Foo</h2>
<h2>Foo</h2>
<h1>Foo</h1>
"###.trim(), "Render mismatch for example 84");
    }
    #[test]
    fn render_example_85() {
        let ast = parse_phrases(r#"    Foo
    ---

    Foo
---
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>Foo
---

Foo
</code></pre>
<hr />
"###.trim(), "Render mismatch for example 85");
    }
    #[test]
    fn render_example_86() {
        let ast = parse_phrases(r#"Foo
   ----      
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h2>Foo</h2>
"###.trim(), "Render mismatch for example 86");
    }
    #[test]
    fn render_example_87() {
        let ast = parse_phrases(r#"Foo
    ---
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo
---</p>
"###.trim(), "Render mismatch for example 87");
    }
    #[test]
    fn render_example_88() {
        let ast = parse_phrases(r#"Foo
= =

Foo
--- -
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo
= =</p>
<p>Foo</p>
<hr />
"###.trim(), "Render mismatch for example 88");
    }
    #[test]
    fn render_example_89() {
        let ast = parse_phrases(r#"Foo  
-----
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h2>Foo</h2>
"###.trim(), "Render mismatch for example 89");
    }
    #[test]
    fn render_example_90() {
        let ast = parse_phrases(r#"Foo\
----
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h2>Foo\</h2>
"###.trim(), "Render mismatch for example 90");
    }
    #[test]
    fn render_example_91() {
        let ast = parse_phrases(r#"`Foo
----
`

<a title=\"a lot
---
of dashes\"/>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h2>`Foo</h2>
<p>`</p>
<h2>&lt;a title=&quot;a lot</h2>
<p>of dashes&quot;/&gt;</p>
"###.trim(), "Render mismatch for example 91");
    }
    #[test]
    fn render_example_92() {
        let ast = parse_phrases(r#"> Foo
---
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>Foo</p>
</blockquote>
<hr />
"###.trim(), "Render mismatch for example 92");
    }
    #[test]
    fn render_example_93() {
        let ast = parse_phrases(r#"> foo
bar
===
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>foo
bar
===</p>
</blockquote>
"###.trim(), "Render mismatch for example 93");
    }
    #[test]
    fn render_example_94() {
        let ast = parse_phrases(r#"- Foo
---
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>Foo</li>
</ul>
<hr />
"###.trim(), "Render mismatch for example 94");
    }
    #[test]
    fn render_example_95() {
        let ast = parse_phrases(r#"Foo
Bar
---
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h2>Foo
Bar</h2>
"###.trim(), "Render mismatch for example 95");
    }
    #[test]
    fn render_example_96() {
        let ast = parse_phrases(r#"---
Foo
---
Bar
---
Baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<hr />
<h2>Foo</h2>
<h2>Bar</h2>
<p>Baz</p>
"###.trim(), "Render mismatch for example 96");
    }
    #[test]
    fn render_example_97() {
        let ast = parse_phrases(r#"
====
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>====</p>
"###.trim(), "Render mismatch for example 97");
    }
    #[test]
    fn render_example_98() {
        let ast = parse_phrases(r#"---
---
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<hr />
<hr />
"###.trim(), "Render mismatch for example 98");
    }
    #[test]
    fn render_example_99() {
        let ast = parse_phrases(r#"- foo
-----
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo</li>
</ul>
<hr />
"###.trim(), "Render mismatch for example 99");
    }
    #[test]
    fn render_example_100() {
        let ast = parse_phrases(r#"    foo
---
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>foo
</code></pre>
<hr />
"###.trim(), "Render mismatch for example 100");
    }
    #[test]
    fn render_example_101() {
        let ast = parse_phrases(r#"> foo
-----
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>foo</p>
</blockquote>
<hr />
"###.trim(), "Render mismatch for example 101");
    }
    #[test]
    fn render_example_102() {
        let ast = parse_phrases(r#"\> foo
------
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h2>&gt; foo</h2>
"###.trim(), "Render mismatch for example 102");
    }
    #[test]
    fn render_example_103() {
        let ast = parse_phrases(r#"Foo

bar
---
baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo</p>
<h2>bar</h2>
<p>baz</p>
"###.trim(), "Render mismatch for example 103");
    }
    #[test]
    fn render_example_104() {
        let ast = parse_phrases(r#"Foo
bar

---

baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo
bar</p>
<hr />
<p>baz</p>
"###.trim(), "Render mismatch for example 104");
    }
    #[test]
    fn render_example_105() {
        let ast = parse_phrases(r#"Foo
bar
* * *
baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo
bar</p>
<hr />
<p>baz</p>
"###.trim(), "Render mismatch for example 105");
    }
    #[test]
    fn render_example_106() {
        let ast = parse_phrases(r#"Foo
bar
\---
baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo
bar
---
baz</p>
"###.trim(), "Render mismatch for example 106");
    }
    #[test]
    fn render_example_107() {
        let ast = parse_phrases(r#"    a simple
      indented code block
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>a simple
  indented code block
</code></pre>
"###.trim(), "Render mismatch for example 107");
    }
    #[test]
    fn render_example_108() {
        let ast = parse_phrases(r#"  - foo

    bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>foo</p>
<p>bar</p>
</li>
</ul>
"###.trim(), "Render mismatch for example 108");
    }
    #[test]
    fn render_example_109() {
        let ast = parse_phrases(r#"1.  foo

    - bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<p>foo</p>
<ul>
<li>bar</li>
</ul>
</li>
</ol>
"###.trim(), "Render mismatch for example 109");
    }
    #[test]
    fn render_example_110() {
        let ast = parse_phrases(r#"    <a/>
    *hi*

    - one
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>&lt;a/&gt;
*hi*

- one
</code></pre>
"###.trim(), "Render mismatch for example 110");
    }
    #[test]
    fn render_example_111() {
        let ast = parse_phrases(r#"    chunk1

    chunk2
  
 
 
    chunk3
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>chunk1

chunk2



chunk3
</code></pre>
"###.trim(), "Render mismatch for example 111");
    }
    #[test]
    fn render_example_112() {
        let ast = parse_phrases(r#"    chunk1
      
      chunk2
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>chunk1
  
  chunk2
</code></pre>
"###.trim(), "Render mismatch for example 112");
    }
    #[test]
    fn render_example_113() {
        let ast = parse_phrases(r#"Foo
    bar

"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo
bar</p>
"###.trim(), "Render mismatch for example 113");
    }
    #[test]
    fn render_example_114() {
        let ast = parse_phrases(r#"    foo
bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>foo
</code></pre>
<p>bar</p>
"###.trim(), "Render mismatch for example 114");
    }
    #[test]
    fn render_example_115() {
        let ast = parse_phrases(r#"# Heading
    foo
Heading
------
    foo
----
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h1>Heading</h1>
<pre><code>foo
</code></pre>
<h2>Heading</h2>
<pre><code>foo
</code></pre>
<hr />
"###.trim(), "Render mismatch for example 115");
    }
    #[test]
    fn render_example_116() {
        let ast = parse_phrases(r#"        foo
    bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>    foo
bar
</code></pre>
"###.trim(), "Render mismatch for example 116");
    }
    #[test]
    fn render_example_117() {
        let ast = parse_phrases(r#"
    
    foo
    

"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>foo
</code></pre>
"###.trim(), "Render mismatch for example 117");
    }
    #[test]
    fn render_example_118() {
        let ast = parse_phrases(r#"    foo  
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>foo  
</code></pre>
"###.trim(), "Render mismatch for example 118");
    }
    #[test]
    fn render_example_119() {
        let ast = parse_phrases(r#"```
<
 >
```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>&lt;
 &gt;
</code></pre>
"###.trim(), "Render mismatch for example 119");
    }
    #[test]
    fn render_example_120() {
        let ast = parse_phrases(r#"~~~
<
 >
~~~
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>&lt;
 &gt;
</code></pre>
"###.trim(), "Render mismatch for example 120");
    }
    #[test]
    fn render_example_121() {
        let ast = parse_phrases(r#"``
foo
``
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>foo</code></p>
"###.trim(), "Render mismatch for example 121");
    }
    #[test]
    fn render_example_122() {
        let ast = parse_phrases(r#"```
aaa
~~~
```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>aaa
~~~
</code></pre>
"###.trim(), "Render mismatch for example 122");
    }
    #[test]
    fn render_example_123() {
        let ast = parse_phrases(r#"~~~
aaa
```
~~~
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>aaa
```
</code></pre>
"###.trim(), "Render mismatch for example 123");
    }
    #[test]
    fn render_example_124() {
        let ast = parse_phrases(r#"````
aaa
```
``````
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>aaa
```
</code></pre>
"###.trim(), "Render mismatch for example 124");
    }
    #[test]
    fn render_example_125() {
        let ast = parse_phrases(r#"~~~~
aaa
~~~
~~~~
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>aaa
~~~
</code></pre>
"###.trim(), "Render mismatch for example 125");
    }
    #[test]
    fn render_example_126() {
        let ast = parse_phrases(r#"```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code></code></pre>
"###.trim(), "Render mismatch for example 126");
    }
    #[test]
    fn render_example_127() {
        let ast = parse_phrases(r#"`````

```
aaa
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>
```
aaa
</code></pre>
"###.trim(), "Render mismatch for example 127");
    }
    #[test]
    fn render_example_128() {
        let ast = parse_phrases(r#"> ```
> aaa

bbb
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<pre><code>aaa
</code></pre>
</blockquote>
<p>bbb</p>
"###.trim(), "Render mismatch for example 128");
    }
    #[test]
    fn render_example_129() {
        let ast = parse_phrases(r#"```

  
```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>
  
</code></pre>
"###.trim(), "Render mismatch for example 129");
    }
    #[test]
    fn render_example_130() {
        let ast = parse_phrases(r#"```
```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code></code></pre>
"###.trim(), "Render mismatch for example 130");
    }
    #[test]
    fn render_example_131() {
        let ast = parse_phrases(r#" ```
 aaa
aaa
```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>aaa
aaa
</code></pre>
"###.trim(), "Render mismatch for example 131");
    }
    #[test]
    fn render_example_132() {
        let ast = parse_phrases(r#"  ```
aaa
  aaa
aaa
  ```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>aaa
aaa
aaa
</code></pre>
"###.trim(), "Render mismatch for example 132");
    }
    #[test]
    fn render_example_133() {
        let ast = parse_phrases(r#"   ```
   aaa
    aaa
  aaa
   ```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>aaa
 aaa
aaa
</code></pre>
"###.trim(), "Render mismatch for example 133");
    }
    #[test]
    fn render_example_134() {
        let ast = parse_phrases(r#"    ```
    aaa
    ```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>```
aaa
```
</code></pre>
"###.trim(), "Render mismatch for example 134");
    }
    #[test]
    fn render_example_135() {
        let ast = parse_phrases(r#"```
aaa
  ```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>aaa
</code></pre>
"###.trim(), "Render mismatch for example 135");
    }
    #[test]
    fn render_example_136() {
        let ast = parse_phrases(r#"   ```
aaa
  ```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>aaa
</code></pre>
"###.trim(), "Render mismatch for example 136");
    }
    #[test]
    fn render_example_137() {
        let ast = parse_phrases(r#"```
aaa
    ```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>aaa
    ```
</code></pre>
"###.trim(), "Render mismatch for example 137");
    }
    #[test]
    fn render_example_138() {
        let ast = parse_phrases(r#"``` ```
aaa
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code> </code>
aaa</p>
"###.trim(), "Render mismatch for example 138");
    }
    #[test]
    fn render_example_139() {
        let ast = parse_phrases(r#"~~~~~~
aaa
~~~ ~~
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>aaa
~~~ ~~
</code></pre>
"###.trim(), "Render mismatch for example 139");
    }
    #[test]
    fn render_example_140() {
        let ast = parse_phrases(r#"foo
```
bar
```
baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo</p>
<pre><code>bar
</code></pre>
<p>baz</p>
"###.trim(), "Render mismatch for example 140");
    }
    #[test]
    fn render_example_141() {
        let ast = parse_phrases(r#"foo
---
~~~
bar
~~~
# baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h2>foo</h2>
<pre><code>bar
</code></pre>
<h1>baz</h1>
"###.trim(), "Render mismatch for example 141");
    }
    #[test]
    fn render_example_142() {
        let ast = parse_phrases(r#"```ruby
def foo(x)
  return 3
end
```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code class="language-ruby">def foo(x)
  return 3
end
</code></pre>
"###.trim(), "Render mismatch for example 142");
    }
    #[test]
    fn render_example_143() {
        let ast = parse_phrases(r#"~~~~    ruby startline=3 $%@#$
def foo(x)
  return 3
end
~~~~~~~
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code class="language-ruby">def foo(x)
  return 3
end
</code></pre>
"###.trim(), "Render mismatch for example 143");
    }
    #[test]
    fn render_example_144() {
        let ast = parse_phrases(r#"````;
````
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code class="language-;"></code></pre>
"###.trim(), "Render mismatch for example 144");
    }
    #[test]
    fn render_example_145() {
        let ast = parse_phrases(r#"``` aa ```
foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>aa</code>
foo</p>
"###.trim(), "Render mismatch for example 145");
    }
    #[test]
    fn render_example_146() {
        let ast = parse_phrases(r#"~~~ aa ``` ~~~
foo
~~~
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code class="language-aa">foo
</code></pre>
"###.trim(), "Render mismatch for example 146");
    }
    #[test]
    fn render_example_147() {
        let ast = parse_phrases(r#"```
``` aaa
```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>``` aaa
</code></pre>
"###.trim(), "Render mismatch for example 147");
    }
    #[test]
    fn render_example_148() {
        let ast = parse_phrases(r#"<table><tr><td>
<pre>
**Hello**,

_world_.
</pre>
</td></tr></table>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<table><tr><td>
<pre>
**Hello**,
<p><em>world</em>.
</pre></p>
</td></tr></table>
"###.trim(), "Render mismatch for example 148");
    }
    #[test]
    fn render_example_149() {
        let ast = parse_phrases(r#"<table>
  <tr>
    <td>
           hi
    </td>
  </tr>
</table>

okay.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<table>
  <tr>
    <td>
           hi
    </td>
  </tr>
</table>
<p>okay.</p>
"###.trim(), "Render mismatch for example 149");
    }
    #[test]
    fn render_example_150() {
        let ast = parse_phrases(r#" <div>
  *hello*
         <foo><a>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###" <div>
  *hello*
         <foo><a>
"###.trim(), "Render mismatch for example 150");
    }
    #[test]
    fn render_example_151() {
        let ast = parse_phrases(r#"</div>
*foo*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"</div>
*foo*
"###.trim(), "Render mismatch for example 151");
    }
    #[test]
    fn render_example_152() {
        let ast = parse_phrases(r#"<DIV CLASS=\"foo\">

*Markdown*

</DIV>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<DIV CLASS="foo">
<p><em>Markdown</em></p>
</DIV>
"###.trim(), "Render mismatch for example 152");
    }
    #[test]
    fn render_example_153() {
        let ast = parse_phrases(r#"<div id=\"foo\"
  class=\"bar\">
</div>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<div id="foo"
  class="bar">
</div>
"###.trim(), "Render mismatch for example 153");
    }
    #[test]
    fn render_example_154() {
        let ast = parse_phrases(r#"<div id=\"foo\" class=\"bar
  baz\">
</div>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<div id="foo" class="bar
  baz">
</div>
"###.trim(), "Render mismatch for example 154");
    }
    #[test]
    fn render_example_155() {
        let ast = parse_phrases(r#"<div>
*foo*

*bar*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<div>
*foo*
<p><em>bar</em></p>
"###.trim(), "Render mismatch for example 155");
    }
    #[test]
    fn render_example_156() {
        let ast = parse_phrases(r#"<div id=\"foo\"
*hi*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<div id="foo"
*hi*
"###.trim(), "Render mismatch for example 156");
    }
    #[test]
    fn render_example_157() {
        let ast = parse_phrases(r#"<div class
foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<div class
foo
"###.trim(), "Render mismatch for example 157");
    }
    #[test]
    fn render_example_158() {
        let ast = parse_phrases(r#"<div *???-&&&-<---
*foo*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<div *???-&&&-<---
*foo*
"###.trim(), "Render mismatch for example 158");
    }
    #[test]
    fn render_example_159() {
        let ast = parse_phrases(r#"<div><a href=\"bar\">*foo*</a></div>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<div><a href="bar">*foo*</a></div>
"###.trim(), "Render mismatch for example 159");
    }
    #[test]
    fn render_example_160() {
        let ast = parse_phrases(r#"<table><tr><td>
foo
</td></tr></table>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<table><tr><td>
foo
</td></tr></table>
"###.trim(), "Render mismatch for example 160");
    }
    #[test]
    fn render_example_161() {
        let ast = parse_phrases(r#"<div></div>
``` c
int x = 33;
```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<div></div>
``` c
int x = 33;
```
"###.trim(), "Render mismatch for example 161");
    }
    #[test]
    fn render_example_162() {
        let ast = parse_phrases(r#"<div
> not quoted text
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<div
> not quoted text
"###.trim(), "Render mismatch for example 162");
    }
    #[test]
    fn render_example_163() {
        let ast = parse_phrases(r#"<a href=\"foo\">
*bar*
</a>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<a href="foo">
*bar*
</a>
"###.trim(), "Render mismatch for example 163");
    }
    #[test]
    fn render_example_164() {
        let ast = parse_phrases(r#"<Warning>
*bar*
</Warning>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<Warning>
*bar*
</Warning>
"###.trim(), "Render mismatch for example 164");
    }
    #[test]
    fn render_example_165() {
        let ast = parse_phrases(r#"<i class=\"foo\">
*bar*
</i>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<i class="foo">
*bar*
</i>
"###.trim(), "Render mismatch for example 165");
    }
    #[test]
    fn render_example_166() {
        let ast = parse_phrases(r#"</ins>
*bar*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"</ins>
*bar*
"###.trim(), "Render mismatch for example 166");
    }
    #[test]
    fn render_example_167() {
        let ast = parse_phrases(r#"<del>
*foo*
</del>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<del>
*foo*
</del>
"###.trim(), "Render mismatch for example 167");
    }
    #[test]
    fn render_example_168() {
        let ast = parse_phrases(r#"<del>

*foo*

</del>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<del>
<p><em>foo</em></p>
</del>
"###.trim(), "Render mismatch for example 168");
    }
    #[test]
    fn render_example_169() {
        let ast = parse_phrases(r#"<del>*foo*</del>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><del><em>foo</em></del></p>
"###.trim(), "Render mismatch for example 169");
    }
    #[test]
    fn render_example_170() {
        let ast = parse_phrases(r#"<del
class=\"foo\">
*foo*
</del>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><del
class="foo">
<em>foo</em>
</del></p>
"###.trim(), "Render mismatch for example 170");
    }
    #[test]
    fn render_example_171() {
        let ast = parse_phrases(r#"<pre language=\"haskell\"><code>
import Text.HTML.TagSoup

main :: IO ()
main = print $ parseTags tags
</code></pre>
okay
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre language="haskell"><code>
import Text.HTML.TagSoup

main :: IO ()
main = print $ parseTags tags
</code></pre>
<p>okay</p>
"###.trim(), "Render mismatch for example 171");
    }
    #[test]
    fn render_example_172() {
        let ast = parse_phrases(r#"<script type=\"text/javascript\">
// JavaScript example

document.getElementById(\"demo\").innerHTML = \"Hello JavaScript!\";
</script>
okay
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<script type="text/javascript">
// JavaScript example

document.getElementById("demo").innerHTML = "Hello JavaScript!";
</script>
<p>okay</p>
"###.trim(), "Render mismatch for example 172");
    }
    #[test]
    fn render_example_173() {
        let ast = parse_phrases(r#"<textarea>

*foo*

_bar_

</textarea>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<textarea>

*foo*

_bar_

</textarea>
"###.trim(), "Render mismatch for example 173");
    }
    #[test]
    fn render_example_174() {
        let ast = parse_phrases(r#"<style
  type=\"text/css\">
h1 {color:red;}

p {color:blue;}
</style>
okay
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<style
  type="text/css">
h1 {color:red;}

p {color:blue;}
</style>
<p>okay</p>
"###.trim(), "Render mismatch for example 174");
    }
    #[test]
    fn render_example_175() {
        let ast = parse_phrases(r#"<style
  type=\"text/css\">

foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<style
  type="text/css">

foo
"###.trim(), "Render mismatch for example 175");
    }
    #[test]
    fn render_example_176() {
        let ast = parse_phrases(r#"> <div>
> foo

bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<div>
foo
</blockquote>
<p>bar</p>
"###.trim(), "Render mismatch for example 176");
    }
    #[test]
    fn render_example_177() {
        let ast = parse_phrases(r#"- <div>
- foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<div>
</li>
<li>foo</li>
</ul>
"###.trim(), "Render mismatch for example 177");
    }
    #[test]
    fn render_example_178() {
        let ast = parse_phrases(r#"<style>p{color:red;}</style>
*foo*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<style>p{color:red;}</style>
<p><em>foo</em></p>
"###.trim(), "Render mismatch for example 178");
    }
    #[test]
    fn render_example_179() {
        let ast = parse_phrases(r#"<!-- foo -->*bar*
*baz*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<!-- foo -->*bar*
<p><em>baz</em></p>
"###.trim(), "Render mismatch for example 179");
    }
    #[test]
    fn render_example_180() {
        let ast = parse_phrases(r#"<script>
foo
</script>1. *bar*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<script>
foo
</script>1. *bar*
"###.trim(), "Render mismatch for example 180");
    }
    #[test]
    fn render_example_181() {
        let ast = parse_phrases(r#"<!-- Foo

bar
   baz -->
okay
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<!-- Foo

bar
   baz -->
<p>okay</p>
"###.trim(), "Render mismatch for example 181");
    }
    #[test]
    fn render_example_182() {
        let ast = parse_phrases(r#"<?php

  echo '>';

?>
okay
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<?php

  echo '>';

?>
<p>okay</p>
"###.trim(), "Render mismatch for example 182");
    }
    #[test]
    fn render_example_183() {
        let ast = parse_phrases(r#"<!DOCTYPE html>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<!DOCTYPE html>
"###.trim(), "Render mismatch for example 183");
    }
    #[test]
    fn render_example_184() {
        let ast = parse_phrases(r#"<![CDATA[
function matchwo(a,b)
{
  if (a < b && a < 0) then {
    return 1;

  } else {

    return 0;
  }
}
]]>
okay
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<![CDATA[
function matchwo(a,b)
{
  if (a < b && a < 0) then {
    return 1;

  } else {

    return 0;
  }
}
]]>
<p>okay</p>
"###.trim(), "Render mismatch for example 184");
    }
    #[test]
    fn render_example_185() {
        let ast = parse_phrases(r#"  <!-- foo -->

    <!-- foo -->
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"  <!-- foo -->
<pre><code>&lt;!-- foo --&gt;
</code></pre>
"###.trim(), "Render mismatch for example 185");
    }
    #[test]
    fn render_example_186() {
        let ast = parse_phrases(r#"  <div>

    <div>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"  <div>
<pre><code>&lt;div&gt;
</code></pre>
"###.trim(), "Render mismatch for example 186");
    }
    #[test]
    fn render_example_187() {
        let ast = parse_phrases(r#"Foo
<div>
bar
</div>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo</p>
<div>
bar
</div>
"###.trim(), "Render mismatch for example 187");
    }
    #[test]
    fn render_example_188() {
        let ast = parse_phrases(r#"<div>
bar
</div>
*foo*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<div>
bar
</div>
*foo*
"###.trim(), "Render mismatch for example 188");
    }
    #[test]
    fn render_example_189() {
        let ast = parse_phrases(r#"Foo
<a href=\"bar\">
baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo
<a href="bar">
baz</p>
"###.trim(), "Render mismatch for example 189");
    }
    #[test]
    fn render_example_190() {
        let ast = parse_phrases(r#"<div>

*Emphasized* text.

</div>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<div>
<p><em>Emphasized</em> text.</p>
</div>
"###.trim(), "Render mismatch for example 190");
    }
    #[test]
    fn render_example_191() {
        let ast = parse_phrases(r#"<div>
*Emphasized* text.
</div>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<div>
*Emphasized* text.
</div>
"###.trim(), "Render mismatch for example 191");
    }
    #[test]
    fn render_example_192() {
        let ast = parse_phrases(r#"<table>

<tr>

<td>
Hi
</td>

</tr>

</table>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<table>
<tr>
<td>
Hi
</td>
</tr>
</table>
"###.trim(), "Render mismatch for example 192");
    }
    #[test]
    fn render_example_193() {
        let ast = parse_phrases(r#"<table>

  <tr>

    <td>
      Hi
    </td>

  </tr>

</table>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<table>
  <tr>
<pre><code>&lt;td&gt;
  Hi
&lt;/td&gt;
</code></pre>
  </tr>
</table>
"###.trim(), "Render mismatch for example 193");
    }
    #[test]
    fn render_example_194() {
        let ast = parse_phrases(r#"[foo]: /url \"title\"

[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title">foo</a></p>
"###.trim(), "Render mismatch for example 194");
    }
    #[test]
    fn render_example_195() {
        let ast = parse_phrases(r#"   [foo]: 
      /url  
           'the title'  

[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="the title">foo</a></p>
"###.trim(), "Render mismatch for example 195");
    }
    #[test]
    fn render_example_196() {
        let ast = parse_phrases(r#"[Foo*bar\]]:my_(url) 'title (with parens)'

[Foo*bar\]]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="my_(url)" title="title (with parens)">Foo*bar]</a></p>
"###.trim(), "Render mismatch for example 196");
    }
    #[test]
    fn render_example_197() {
        let ast = parse_phrases(r#"[Foo bar]:
<my url>
'title'

[Foo bar]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="my%20url" title="title">Foo bar</a></p>
"###.trim(), "Render mismatch for example 197");
    }
    #[test]
    fn render_example_198() {
        let ast = parse_phrases(r#"[foo]: /url '
title
line1
line2
'

[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="
title
line1
line2
">foo</a></p>
"###.trim(), "Render mismatch for example 198");
    }
    #[test]
    fn render_example_199() {
        let ast = parse_phrases(r#"[foo]: /url 'title

with blank line'

[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo]: /url 'title</p>
<p>with blank line'</p>
<p>[foo]</p>
"###.trim(), "Render mismatch for example 199");
    }
    #[test]
    fn render_example_200() {
        let ast = parse_phrases(r#"[foo]:
/url

[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url">foo</a></p>
"###.trim(), "Render mismatch for example 200");
    }
    #[test]
    fn render_example_201() {
        let ast = parse_phrases(r#"[foo]:

[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo]:</p>
<p>[foo]</p>
"###.trim(), "Render mismatch for example 201");
    }
    #[test]
    fn render_example_202() {
        let ast = parse_phrases(r#"[foo]: <>

[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="">foo</a></p>
"###.trim(), "Render mismatch for example 202");
    }
    #[test]
    fn render_example_203() {
        let ast = parse_phrases(r#"[foo]: <bar>(baz)

[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo]: <bar>(baz)</p>
<p>[foo]</p>
"###.trim(), "Render mismatch for example 203");
    }
    #[test]
    fn render_example_204() {
        let ast = parse_phrases(r#"[foo]: /url\bar\*baz \"foo\\"bar\baz\"

[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url%5Cbar*baz" title="foo&quot;bar\baz">foo</a></p>
"###.trim(), "Render mismatch for example 204");
    }
    #[test]
    fn render_example_205() {
        let ast = parse_phrases(r#"[foo]

[foo]: url
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="url">foo</a></p>
"###.trim(), "Render mismatch for example 205");
    }
    #[test]
    fn render_example_206() {
        let ast = parse_phrases(r#"[foo]

[foo]: first
[foo]: second
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="first">foo</a></p>
"###.trim(), "Render mismatch for example 206");
    }
    #[test]
    fn render_example_207() {
        let ast = parse_phrases(r#"[FOO]: /url

[Foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url">Foo</a></p>
"###.trim(), "Render mismatch for example 207");
    }
    #[test]
    fn render_example_208() {
        let ast = parse_phrases(r#"[ΑΓΩ]: /φου

[αγω]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/%CF%86%CE%BF%CF%85">αγω</a></p>
"###.trim(), "Render mismatch for example 208");
    }
    #[test]
    fn render_example_209() {
        let ast = parse_phrases(r#"[foo]: /url
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###""###.trim(), "Render mismatch for example 209");
    }
    #[test]
    fn render_example_210() {
        let ast = parse_phrases(r#"[
foo
]: /url
bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>bar</p>
"###.trim(), "Render mismatch for example 210");
    }
    #[test]
    fn render_example_211() {
        let ast = parse_phrases(r#"[foo]: /url \"title\" ok
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo]: /url &quot;title&quot; ok</p>
"###.trim(), "Render mismatch for example 211");
    }
    #[test]
    fn render_example_212() {
        let ast = parse_phrases(r#"[foo]: /url
\"title\" ok
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&quot;title&quot; ok</p>
"###.trim(), "Render mismatch for example 212");
    }
    #[test]
    fn render_example_213() {
        let ast = parse_phrases(r#"    [foo]: /url \"title\"

[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>[foo]: /url &quot;title&quot;
</code></pre>
<p>[foo]</p>
"###.trim(), "Render mismatch for example 213");
    }
    #[test]
    fn render_example_214() {
        let ast = parse_phrases(r#"```
[foo]: /url
```

[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>[foo]: /url
</code></pre>
<p>[foo]</p>
"###.trim(), "Render mismatch for example 214");
    }
    #[test]
    fn render_example_215() {
        let ast = parse_phrases(r#"Foo
[bar]: /baz

[bar]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo
[bar]: /baz</p>
<p>[bar]</p>
"###.trim(), "Render mismatch for example 215");
    }
    #[test]
    fn render_example_216() {
        let ast = parse_phrases(r#"# [Foo]
[foo]: /url
> bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h1><a href="/url">Foo</a></h1>
<blockquote>
<p>bar</p>
</blockquote>
"###.trim(), "Render mismatch for example 216");
    }
    #[test]
    fn render_example_217() {
        let ast = parse_phrases(r#"[foo]: /url
bar
===
[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h1>bar</h1>
<p><a href="/url">foo</a></p>
"###.trim(), "Render mismatch for example 217");
    }
    #[test]
    fn render_example_218() {
        let ast = parse_phrases(r#"[foo]: /url
===
[foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>===
<a href="/url">foo</a></p>
"###.trim(), "Render mismatch for example 218");
    }
    #[test]
    fn render_example_219() {
        let ast = parse_phrases(r#"[foo]: /foo-url \"foo\"
[bar]: /bar-url
  \"bar\"
[baz]: /baz-url

[foo],
[bar],
[baz]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/foo-url" title="foo">foo</a>,
<a href="/bar-url" title="bar">bar</a>,
<a href="/baz-url">baz</a></p>
"###.trim(), "Render mismatch for example 219");
    }
    #[test]
    fn render_example_220() {
        let ast = parse_phrases(r#"[foo]

> [foo]: /url
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url">foo</a></p>
<blockquote>
</blockquote>
"###.trim(), "Render mismatch for example 220");
    }
    #[test]
    fn render_example_221() {
        let ast = parse_phrases(r#"aaa

bbb
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>aaa</p>
<p>bbb</p>
"###.trim(), "Render mismatch for example 221");
    }
    #[test]
    fn render_example_222() {
        let ast = parse_phrases(r#"aaa
bbb

ccc
ddd
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>aaa
bbb</p>
<p>ccc
ddd</p>
"###.trim(), "Render mismatch for example 222");
    }
    #[test]
    fn render_example_223() {
        let ast = parse_phrases(r#"aaa


bbb
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>aaa</p>
<p>bbb</p>
"###.trim(), "Render mismatch for example 223");
    }
    #[test]
    fn render_example_224() {
        let ast = parse_phrases(r#"  aaa
 bbb
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>aaa
bbb</p>
"###.trim(), "Render mismatch for example 224");
    }
    #[test]
    fn render_example_225() {
        let ast = parse_phrases(r#"aaa
             bbb
                                       ccc
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>aaa
bbb
ccc</p>
"###.trim(), "Render mismatch for example 225");
    }
    #[test]
    fn render_example_226() {
        let ast = parse_phrases(r#"   aaa
bbb
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>aaa
bbb</p>
"###.trim(), "Render mismatch for example 226");
    }
    #[test]
    fn render_example_227() {
        let ast = parse_phrases(r#"    aaa
bbb
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>aaa
</code></pre>
<p>bbb</p>
"###.trim(), "Render mismatch for example 227");
    }
    #[test]
    fn render_example_228() {
        let ast = parse_phrases(r#"aaa     
bbb     
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>aaa<br />
bbb</p>
"###.trim(), "Render mismatch for example 228");
    }
    #[test]
    fn render_example_229() {
        let ast = parse_phrases(r#"  

aaa
  

# aaa

  
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>aaa</p>
<h1>aaa</h1>
"###.trim(), "Render mismatch for example 229");
    }
    #[test]
    fn render_example_230() {
        let ast = parse_phrases(r#"> # Foo
> bar
> baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<h1>Foo</h1>
<p>bar
baz</p>
</blockquote>
"###.trim(), "Render mismatch for example 230");
    }
    #[test]
    fn render_example_231() {
        let ast = parse_phrases(r#"># Foo
>bar
> baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<h1>Foo</h1>
<p>bar
baz</p>
</blockquote>
"###.trim(), "Render mismatch for example 231");
    }
    #[test]
    fn render_example_232() {
        let ast = parse_phrases(r#"   > # Foo
   > bar
 > baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<h1>Foo</h1>
<p>bar
baz</p>
</blockquote>
"###.trim(), "Render mismatch for example 232");
    }
    #[test]
    fn render_example_233() {
        let ast = parse_phrases(r#"    > # Foo
    > bar
    > baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>&gt; # Foo
&gt; bar
&gt; baz
</code></pre>
"###.trim(), "Render mismatch for example 233");
    }
    #[test]
    fn render_example_234() {
        let ast = parse_phrases(r#"> # Foo
> bar
baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<h1>Foo</h1>
<p>bar
baz</p>
</blockquote>
"###.trim(), "Render mismatch for example 234");
    }
    #[test]
    fn render_example_235() {
        let ast = parse_phrases(r#"> bar
baz
> foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>bar
baz
foo</p>
</blockquote>
"###.trim(), "Render mismatch for example 235");
    }
    #[test]
    fn render_example_236() {
        let ast = parse_phrases(r#"> foo
---
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>foo</p>
</blockquote>
<hr />
"###.trim(), "Render mismatch for example 236");
    }
    #[test]
    fn render_example_237() {
        let ast = parse_phrases(r#"> - foo
- bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<ul>
<li>foo</li>
</ul>
</blockquote>
<ul>
<li>bar</li>
</ul>
"###.trim(), "Render mismatch for example 237");
    }
    #[test]
    fn render_example_238() {
        let ast = parse_phrases(r#">     foo
    bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<pre><code>foo
</code></pre>
</blockquote>
<pre><code>bar
</code></pre>
"###.trim(), "Render mismatch for example 238");
    }
    #[test]
    fn render_example_239() {
        let ast = parse_phrases(r#"> ```
foo
```
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<pre><code></code></pre>
</blockquote>
<p>foo</p>
<pre><code></code></pre>
"###.trim(), "Render mismatch for example 239");
    }
    #[test]
    fn render_example_240() {
        let ast = parse_phrases(r#"> foo
    - bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>foo
- bar</p>
</blockquote>
"###.trim(), "Render mismatch for example 240");
    }
    #[test]
    fn render_example_241() {
        let ast = parse_phrases(r#">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
</blockquote>
"###.trim(), "Render mismatch for example 241");
    }
    #[test]
    fn render_example_242() {
        let ast = parse_phrases(r#">
>  
> 
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
</blockquote>
"###.trim(), "Render mismatch for example 242");
    }
    #[test]
    fn render_example_243() {
        let ast = parse_phrases(r#">
> foo
>  
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>foo</p>
</blockquote>
"###.trim(), "Render mismatch for example 243");
    }
    #[test]
    fn render_example_244() {
        let ast = parse_phrases(r#"> foo

> bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>foo</p>
</blockquote>
<blockquote>
<p>bar</p>
</blockquote>
"###.trim(), "Render mismatch for example 244");
    }
    #[test]
    fn render_example_245() {
        let ast = parse_phrases(r#"> foo
> bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>foo
bar</p>
</blockquote>
"###.trim(), "Render mismatch for example 245");
    }
    #[test]
    fn render_example_246() {
        let ast = parse_phrases(r#"> foo
>
> bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>foo</p>
<p>bar</p>
</blockquote>
"###.trim(), "Render mismatch for example 246");
    }
    #[test]
    fn render_example_247() {
        let ast = parse_phrases(r#"foo
> bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo</p>
<blockquote>
<p>bar</p>
</blockquote>
"###.trim(), "Render mismatch for example 247");
    }
    #[test]
    fn render_example_248() {
        let ast = parse_phrases(r#"> aaa
***
> bbb
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>aaa</p>
</blockquote>
<hr />
<blockquote>
<p>bbb</p>
</blockquote>
"###.trim(), "Render mismatch for example 248");
    }
    #[test]
    fn render_example_249() {
        let ast = parse_phrases(r#"> bar
baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>bar
baz</p>
</blockquote>
"###.trim(), "Render mismatch for example 249");
    }
    #[test]
    fn render_example_250() {
        let ast = parse_phrases(r#"> bar

baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>bar</p>
</blockquote>
<p>baz</p>
"###.trim(), "Render mismatch for example 250");
    }
    #[test]
    fn render_example_251() {
        let ast = parse_phrases(r#"> bar
>
baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<p>bar</p>
</blockquote>
<p>baz</p>
"###.trim(), "Render mismatch for example 251");
    }
    #[test]
    fn render_example_252() {
        let ast = parse_phrases(r#"> > > foo
bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<blockquote>
<blockquote>
<p>foo
bar</p>
</blockquote>
</blockquote>
</blockquote>
"###.trim(), "Render mismatch for example 252");
    }
    #[test]
    fn render_example_253() {
        let ast = parse_phrases(r#">>> foo
> bar
>>baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<blockquote>
<blockquote>
<p>foo
bar
baz</p>
</blockquote>
</blockquote>
</blockquote>
"###.trim(), "Render mismatch for example 253");
    }
    #[test]
    fn render_example_254() {
        let ast = parse_phrases(r#">     code

>    not code
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<pre><code>code
</code></pre>
</blockquote>
<blockquote>
<p>not code</p>
</blockquote>
"###.trim(), "Render mismatch for example 254");
    }
    #[test]
    fn render_example_255() {
        let ast = parse_phrases(r#"A paragraph
with two lines.

    indented code

> A block quote.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>A paragraph
with two lines.</p>
<pre><code>indented code
</code></pre>
<blockquote>
<p>A block quote.</p>
</blockquote>
"###.trim(), "Render mismatch for example 255");
    }
    #[test]
    fn render_example_256() {
        let ast = parse_phrases(r#"1.  A paragraph
    with two lines.

        indented code

    > A block quote.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<p>A paragraph
with two lines.</p>
<pre><code>indented code
</code></pre>
<blockquote>
<p>A block quote.</p>
</blockquote>
</li>
</ol>
"###.trim(), "Render mismatch for example 256");
    }
    #[test]
    fn render_example_257() {
        let ast = parse_phrases(r#"- one

 two
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>one</li>
</ul>
<p>two</p>
"###.trim(), "Render mismatch for example 257");
    }
    #[test]
    fn render_example_258() {
        let ast = parse_phrases(r#"- one

  two
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>one</p>
<p>two</p>
</li>
</ul>
"###.trim(), "Render mismatch for example 258");
    }
    #[test]
    fn render_example_259() {
        let ast = parse_phrases(r#" -    one

     two
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>one</li>
</ul>
<pre><code> two
</code></pre>
"###.trim(), "Render mismatch for example 259");
    }
    #[test]
    fn render_example_260() {
        let ast = parse_phrases(r#" -    one

      two
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>one</p>
<p>two</p>
</li>
</ul>
"###.trim(), "Render mismatch for example 260");
    }
    #[test]
    fn render_example_261() {
        let ast = parse_phrases(r#"   > > 1.  one
>>
>>     two
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<blockquote>
<ol>
<li>
<p>one</p>
<p>two</p>
</li>
</ol>
</blockquote>
</blockquote>
"###.trim(), "Render mismatch for example 261");
    }
    #[test]
    fn render_example_262() {
        let ast = parse_phrases(r#">>- one
>>
  >  > two
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<blockquote>
<ul>
<li>one</li>
</ul>
<p>two</p>
</blockquote>
</blockquote>
"###.trim(), "Render mismatch for example 262");
    }
    #[test]
    fn render_example_263() {
        let ast = parse_phrases(r#"-one

2.two
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>-one</p>
<p>2.two</p>
"###.trim(), "Render mismatch for example 263");
    }
    #[test]
    fn render_example_264() {
        let ast = parse_phrases(r#"- foo


  bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>foo</p>
<p>bar</p>
</li>
</ul>
"###.trim(), "Render mismatch for example 264");
    }
    #[test]
    fn render_example_265() {
        let ast = parse_phrases(r#"1.  foo

    ```
    bar
    ```

    baz

    > bam
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<p>foo</p>
<pre><code>bar
</code></pre>
<p>baz</p>
<blockquote>
<p>bam</p>
</blockquote>
</li>
</ol>
"###.trim(), "Render mismatch for example 265");
    }
    #[test]
    fn render_example_266() {
        let ast = parse_phrases(r#"- Foo

      bar


      baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>Foo</p>
<pre><code>bar


baz
</code></pre>
</li>
</ul>
"###.trim(), "Render mismatch for example 266");
    }
    #[test]
    fn render_example_267() {
        let ast = parse_phrases(r#"123456789. ok
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol start="123456789">
<li>ok</li>
</ol>
"###.trim(), "Render mismatch for example 267");
    }
    #[test]
    fn render_example_268() {
        let ast = parse_phrases(r#"1234567890. not ok
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>1234567890. not ok</p>
"###.trim(), "Render mismatch for example 268");
    }
    #[test]
    fn render_example_269() {
        let ast = parse_phrases(r#"0. ok
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol start="0">
<li>ok</li>
</ol>
"###.trim(), "Render mismatch for example 269");
    }
    #[test]
    fn render_example_270() {
        let ast = parse_phrases(r#"003. ok
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol start="3">
<li>ok</li>
</ol>
"###.trim(), "Render mismatch for example 270");
    }
    #[test]
    fn render_example_271() {
        let ast = parse_phrases(r#"-1. not ok
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>-1. not ok</p>
"###.trim(), "Render mismatch for example 271");
    }
    #[test]
    fn render_example_272() {
        let ast = parse_phrases(r#"- foo

      bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>foo</p>
<pre><code>bar
</code></pre>
</li>
</ul>
"###.trim(), "Render mismatch for example 272");
    }
    #[test]
    fn render_example_273() {
        let ast = parse_phrases(r#"  10.  foo

           bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol start="10">
<li>
<p>foo</p>
<pre><code>bar
</code></pre>
</li>
</ol>
"###.trim(), "Render mismatch for example 273");
    }
    #[test]
    fn render_example_274() {
        let ast = parse_phrases(r#"    indented code

paragraph

    more code
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>indented code
</code></pre>
<p>paragraph</p>
<pre><code>more code
</code></pre>
"###.trim(), "Render mismatch for example 274");
    }
    #[test]
    fn render_example_275() {
        let ast = parse_phrases(r#"1.     indented code

   paragraph

       more code
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<pre><code>indented code
</code></pre>
<p>paragraph</p>
<pre><code>more code
</code></pre>
</li>
</ol>
"###.trim(), "Render mismatch for example 275");
    }
    #[test]
    fn render_example_276() {
        let ast = parse_phrases(r#"1.      indented code

   paragraph

       more code
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<pre><code> indented code
</code></pre>
<p>paragraph</p>
<pre><code>more code
</code></pre>
</li>
</ol>
"###.trim(), "Render mismatch for example 276");
    }
    #[test]
    fn render_example_277() {
        let ast = parse_phrases(r#"   foo

bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo</p>
<p>bar</p>
"###.trim(), "Render mismatch for example 277");
    }
    #[test]
    fn render_example_278() {
        let ast = parse_phrases(r#"-    foo

  bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo</li>
</ul>
<p>bar</p>
"###.trim(), "Render mismatch for example 278");
    }
    #[test]
    fn render_example_279() {
        let ast = parse_phrases(r#"-  foo

   bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>foo</p>
<p>bar</p>
</li>
</ul>
"###.trim(), "Render mismatch for example 279");
    }
    #[test]
    fn render_example_280() {
        let ast = parse_phrases(r#"-
  foo
-
  ```
  bar
  ```
-
      baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo</li>
<li>
<pre><code>bar
</code></pre>
</li>
<li>
<pre><code>baz
</code></pre>
</li>
</ul>
"###.trim(), "Render mismatch for example 280");
    }
    #[test]
    fn render_example_281() {
        let ast = parse_phrases(r#"-   
  foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo</li>
</ul>
"###.trim(), "Render mismatch for example 281");
    }
    #[test]
    fn render_example_282() {
        let ast = parse_phrases(r#"-

  foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li></li>
</ul>
<p>foo</p>
"###.trim(), "Render mismatch for example 282");
    }
    #[test]
    fn render_example_283() {
        let ast = parse_phrases(r#"- foo
-
- bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo</li>
<li></li>
<li>bar</li>
</ul>
"###.trim(), "Render mismatch for example 283");
    }
    #[test]
    fn render_example_284() {
        let ast = parse_phrases(r#"- foo
-   
- bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo</li>
<li></li>
<li>bar</li>
</ul>
"###.trim(), "Render mismatch for example 284");
    }
    #[test]
    fn render_example_285() {
        let ast = parse_phrases(r#"1. foo
2.
3. bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>foo</li>
<li></li>
<li>bar</li>
</ol>
"###.trim(), "Render mismatch for example 285");
    }
    #[test]
    fn render_example_286() {
        let ast = parse_phrases(r#"*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li></li>
</ul>
"###.trim(), "Render mismatch for example 286");
    }
    #[test]
    fn render_example_287() {
        let ast = parse_phrases(r#"foo
*

foo
1.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo
*</p>
<p>foo
1.</p>
"###.trim(), "Render mismatch for example 287");
    }
    #[test]
    fn render_example_288() {
        let ast = parse_phrases(r#" 1.  A paragraph
     with two lines.

         indented code

     > A block quote.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<p>A paragraph
with two lines.</p>
<pre><code>indented code
</code></pre>
<blockquote>
<p>A block quote.</p>
</blockquote>
</li>
</ol>
"###.trim(), "Render mismatch for example 288");
    }
    #[test]
    fn render_example_289() {
        let ast = parse_phrases(r#"  1.  A paragraph
      with two lines.

          indented code

      > A block quote.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<p>A paragraph
with two lines.</p>
<pre><code>indented code
</code></pre>
<blockquote>
<p>A block quote.</p>
</blockquote>
</li>
</ol>
"###.trim(), "Render mismatch for example 289");
    }
    #[test]
    fn render_example_290() {
        let ast = parse_phrases(r#"   1.  A paragraph
       with two lines.

           indented code

       > A block quote.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<p>A paragraph
with two lines.</p>
<pre><code>indented code
</code></pre>
<blockquote>
<p>A block quote.</p>
</blockquote>
</li>
</ol>
"###.trim(), "Render mismatch for example 290");
    }
    #[test]
    fn render_example_291() {
        let ast = parse_phrases(r#"    1.  A paragraph
        with two lines.

            indented code

        > A block quote.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<pre><code>1.  A paragraph
    with two lines.

        indented code

    &gt; A block quote.
</code></pre>
"###.trim(), "Render mismatch for example 291");
    }
    #[test]
    fn render_example_292() {
        let ast = parse_phrases(r#"  1.  A paragraph
with two lines.

          indented code

      > A block quote.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<p>A paragraph
with two lines.</p>
<pre><code>indented code
</code></pre>
<blockquote>
<p>A block quote.</p>
</blockquote>
</li>
</ol>
"###.trim(), "Render mismatch for example 292");
    }
    #[test]
    fn render_example_293() {
        let ast = parse_phrases(r#"  1.  A paragraph
    with two lines.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>A paragraph
with two lines.</li>
</ol>
"###.trim(), "Render mismatch for example 293");
    }
    #[test]
    fn render_example_294() {
        let ast = parse_phrases(r#"> 1. > Blockquote
continued here.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<ol>
<li>
<blockquote>
<p>Blockquote
continued here.</p>
</blockquote>
</li>
</ol>
</blockquote>
"###.trim(), "Render mismatch for example 294");
    }
    #[test]
    fn render_example_295() {
        let ast = parse_phrases(r#"> 1. > Blockquote
> continued here.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<blockquote>
<ol>
<li>
<blockquote>
<p>Blockquote
continued here.</p>
</blockquote>
</li>
</ol>
</blockquote>
"###.trim(), "Render mismatch for example 295");
    }
    #[test]
    fn render_example_296() {
        let ast = parse_phrases(r#"- foo
  - bar
    - baz
      - boo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo
<ul>
<li>bar
<ul>
<li>baz
<ul>
<li>boo</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
"###.trim(), "Render mismatch for example 296");
    }
    #[test]
    fn render_example_297() {
        let ast = parse_phrases(r#"- foo
 - bar
  - baz
   - boo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo</li>
<li>bar</li>
<li>baz</li>
<li>boo</li>
</ul>
"###.trim(), "Render mismatch for example 297");
    }
    #[test]
    fn render_example_298() {
        let ast = parse_phrases(r#"10) foo
    - bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol start="10">
<li>foo
<ul>
<li>bar</li>
</ul>
</li>
</ol>
"###.trim(), "Render mismatch for example 298");
    }
    #[test]
    fn render_example_299() {
        let ast = parse_phrases(r#"10) foo
   - bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol start="10">
<li>foo</li>
</ol>
<ul>
<li>bar</li>
</ul>
"###.trim(), "Render mismatch for example 299");
    }
    #[test]
    fn render_example_300() {
        let ast = parse_phrases(r#"- - foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<ul>
<li>foo</li>
</ul>
</li>
</ul>
"###.trim(), "Render mismatch for example 300");
    }
    #[test]
    fn render_example_301() {
        let ast = parse_phrases(r#"1. - 2. foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<ul>
<li>
<ol start="2">
<li>foo</li>
</ol>
</li>
</ul>
</li>
</ol>
"###.trim(), "Render mismatch for example 301");
    }
    #[test]
    fn render_example_302() {
        let ast = parse_phrases(r#"- # Foo
- Bar
  ---
  baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<h1>Foo</h1>
</li>
<li>
<h2>Bar</h2>
baz</li>
</ul>
"###.trim(), "Render mismatch for example 302");
    }
    #[test]
    fn render_example_303() {
        let ast = parse_phrases(r#"- foo
- bar
+ baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo</li>
<li>bar</li>
</ul>
<ul>
<li>baz</li>
</ul>
"###.trim(), "Render mismatch for example 303");
    }
    #[test]
    fn render_example_304() {
        let ast = parse_phrases(r#"1. foo
2. bar
3) baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>foo</li>
<li>bar</li>
</ol>
<ol start="3">
<li>baz</li>
</ol>
"###.trim(), "Render mismatch for example 304");
    }
    #[test]
    fn render_example_305() {
        let ast = parse_phrases(r#"Foo
- bar
- baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo</p>
<ul>
<li>bar</li>
<li>baz</li>
</ul>
"###.trim(), "Render mismatch for example 305");
    }
    #[test]
    fn render_example_306() {
        let ast = parse_phrases(r#"The number of windows in my house is
14.  The number of doors is 6.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>The number of windows in my house is
14.  The number of doors is 6.</p>
"###.trim(), "Render mismatch for example 306");
    }
    #[test]
    fn render_example_307() {
        let ast = parse_phrases(r#"The number of windows in my house is
1.  The number of doors is 6.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>The number of windows in my house is</p>
<ol>
<li>The number of doors is 6.</li>
</ol>
"###.trim(), "Render mismatch for example 307");
    }
    #[test]
    fn render_example_308() {
        let ast = parse_phrases(r#"- foo

- bar


- baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>foo</p>
</li>
<li>
<p>bar</p>
</li>
<li>
<p>baz</p>
</li>
</ul>
"###.trim(), "Render mismatch for example 308");
    }
    #[test]
    fn render_example_309() {
        let ast = parse_phrases(r#"- foo
  - bar
    - baz


      bim
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo
<ul>
<li>bar
<ul>
<li>
<p>baz</p>
<p>bim</p>
</li>
</ul>
</li>
</ul>
</li>
</ul>
"###.trim(), "Render mismatch for example 309");
    }
    #[test]
    fn render_example_310() {
        let ast = parse_phrases(r#"- foo
- bar

<!-- -->

- baz
- bim
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>foo</li>
<li>bar</li>
</ul>
<!-- -->
<ul>
<li>baz</li>
<li>bim</li>
</ul>
"###.trim(), "Render mismatch for example 310");
    }
    #[test]
    fn render_example_311() {
        let ast = parse_phrases(r#"-   foo

    notcode

-   foo

<!-- -->

    code
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>foo</p>
<p>notcode</p>
</li>
<li>
<p>foo</p>
</li>
</ul>
<!-- -->
<pre><code>code
</code></pre>
"###.trim(), "Render mismatch for example 311");
    }
    #[test]
    fn render_example_312() {
        let ast = parse_phrases(r#"- a
 - b
  - c
   - d
  - e
 - f
- g
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>a</li>
<li>b</li>
<li>c</li>
<li>d</li>
<li>e</li>
<li>f</li>
<li>g</li>
</ul>
"###.trim(), "Render mismatch for example 312");
    }
    #[test]
    fn render_example_313() {
        let ast = parse_phrases(r#"1. a

  2. b

   3. c
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<p>a</p>
</li>
<li>
<p>b</p>
</li>
<li>
<p>c</p>
</li>
</ol>
"###.trim(), "Render mismatch for example 313");
    }
    #[test]
    fn render_example_314() {
        let ast = parse_phrases(r#"- a
 - b
  - c
   - d
    - e
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>a</li>
<li>b</li>
<li>c</li>
<li>d
- e</li>
</ul>
"###.trim(), "Render mismatch for example 314");
    }
    #[test]
    fn render_example_315() {
        let ast = parse_phrases(r#"1. a

  2. b

    3. c
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<p>a</p>
</li>
<li>
<p>b</p>
</li>
</ol>
<pre><code>3. c
</code></pre>
"###.trim(), "Render mismatch for example 315");
    }
    #[test]
    fn render_example_316() {
        let ast = parse_phrases(r#"- a
- b

- c
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>a</p>
</li>
<li>
<p>b</p>
</li>
<li>
<p>c</p>
</li>
</ul>
"###.trim(), "Render mismatch for example 316");
    }
    #[test]
    fn render_example_317() {
        let ast = parse_phrases(r#"* a
*

* c
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>a</p>
</li>
<li></li>
<li>
<p>c</p>
</li>
</ul>
"###.trim(), "Render mismatch for example 317");
    }
    #[test]
    fn render_example_318() {
        let ast = parse_phrases(r#"- a
- b

  c
- d
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>a</p>
</li>
<li>
<p>b</p>
<p>c</p>
</li>
<li>
<p>d</p>
</li>
</ul>
"###.trim(), "Render mismatch for example 318");
    }
    #[test]
    fn render_example_319() {
        let ast = parse_phrases(r#"- a
- b

  [ref]: /url
- d
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>a</p>
</li>
<li>
<p>b</p>
</li>
<li>
<p>d</p>
</li>
</ul>
"###.trim(), "Render mismatch for example 319");
    }
    #[test]
    fn render_example_320() {
        let ast = parse_phrases(r#"- a
- ```
  b


  ```
- c
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>a</li>
<li>
<pre><code>b


</code></pre>
</li>
<li>c</li>
</ul>
"###.trim(), "Render mismatch for example 320");
    }
    #[test]
    fn render_example_321() {
        let ast = parse_phrases(r#"- a
  - b

    c
- d
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>a
<ul>
<li>
<p>b</p>
<p>c</p>
</li>
</ul>
</li>
<li>d</li>
</ul>
"###.trim(), "Render mismatch for example 321");
    }
    #[test]
    fn render_example_322() {
        let ast = parse_phrases(r#"* a
  > b
  >
* c
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>a
<blockquote>
<p>b</p>
</blockquote>
</li>
<li>c</li>
</ul>
"###.trim(), "Render mismatch for example 322");
    }
    #[test]
    fn render_example_323() {
        let ast = parse_phrases(r#"- a
  > b
  ```
  c
  ```
- d
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>a
<blockquote>
<p>b</p>
</blockquote>
<pre><code>c
</code></pre>
</li>
<li>d</li>
</ul>
"###.trim(), "Render mismatch for example 323");
    }
    #[test]
    fn render_example_324() {
        let ast = parse_phrases(r#"- a
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>a</li>
</ul>
"###.trim(), "Render mismatch for example 324");
    }
    #[test]
    fn render_example_325() {
        let ast = parse_phrases(r#"- a
  - b
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>a
<ul>
<li>b</li>
</ul>
</li>
</ul>
"###.trim(), "Render mismatch for example 325");
    }
    #[test]
    fn render_example_326() {
        let ast = parse_phrases(r#"1. ```
   foo
   ```

   bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ol>
<li>
<pre><code>foo
</code></pre>
<p>bar</p>
</li>
</ol>
"###.trim(), "Render mismatch for example 326");
    }
    #[test]
    fn render_example_327() {
        let ast = parse_phrases(r#"* foo
  * bar

  baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>foo</p>
<ul>
<li>bar</li>
</ul>
<p>baz</p>
</li>
</ul>
"###.trim(), "Render mismatch for example 327");
    }
    #[test]
    fn render_example_328() {
        let ast = parse_phrases(r#"- a
  - b
  - c

- d
  - e
  - f
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<ul>
<li>
<p>a</p>
<ul>
<li>b</li>
<li>c</li>
</ul>
</li>
<li>
<p>d</p>
<ul>
<li>e</li>
<li>f</li>
</ul>
</li>
</ul>
"###.trim(), "Render mismatch for example 328");
    }
    #[test]
    fn render_example_329() {
        let ast = parse_phrases(r#"`hi`lo`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>hi</code>lo`</p>
"###.trim(), "Render mismatch for example 329");
    }
    #[test]
    fn render_example_330() {
        let ast = parse_phrases(r#"`foo`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>foo</code></p>
"###.trim(), "Render mismatch for example 330");
    }
    #[test]
    fn render_example_331() {
        let ast = parse_phrases(r#"`` foo ` bar ``
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>foo ` bar</code></p>
"###.trim(), "Render mismatch for example 331");
    }
    #[test]
    fn render_example_332() {
        let ast = parse_phrases(r#"` `` `
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>``</code></p>
"###.trim(), "Render mismatch for example 332");
    }
    #[test]
    fn render_example_333() {
        let ast = parse_phrases(r#"`  ``  `
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code> `` </code></p>
"###.trim(), "Render mismatch for example 333");
    }
    #[test]
    fn render_example_334() {
        let ast = parse_phrases(r#"` a`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code> a</code></p>
"###.trim(), "Render mismatch for example 334");
    }
    #[test]
    fn render_example_335() {
        let ast = parse_phrases(r#"` b `
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code> b </code></p>
"###.trim(), "Render mismatch for example 335");
    }
    #[test]
    fn render_example_336() {
        let ast = parse_phrases(r#"` `
`  `
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code> </code>
<code>  </code></p>
"###.trim(), "Render mismatch for example 336");
    }
    #[test]
    fn render_example_337() {
        let ast = parse_phrases(r#"``
foo
bar  
baz
``
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>foo bar   baz</code></p>
"###.trim(), "Render mismatch for example 337");
    }
    #[test]
    fn render_example_338() {
        let ast = parse_phrases(r#"``
foo 
``
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>foo </code></p>
"###.trim(), "Render mismatch for example 338");
    }
    #[test]
    fn render_example_339() {
        let ast = parse_phrases(r#"`foo   bar 
baz`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>foo   bar  baz</code></p>
"###.trim(), "Render mismatch for example 339");
    }
    #[test]
    fn render_example_340() {
        let ast = parse_phrases(r#"`foo\`bar`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>foo\</code>bar`</p>
"###.trim(), "Render mismatch for example 340");
    }
    #[test]
    fn render_example_341() {
        let ast = parse_phrases(r#"``foo`bar``
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>foo`bar</code></p>
"###.trim(), "Render mismatch for example 341");
    }
    #[test]
    fn render_example_342() {
        let ast = parse_phrases(r#"` foo `` bar `
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>foo `` bar</code></p>
"###.trim(), "Render mismatch for example 342");
    }
    #[test]
    fn render_example_343() {
        let ast = parse_phrases(r#"*foo`*`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*foo<code>*</code></p>
"###.trim(), "Render mismatch for example 343");
    }
    #[test]
    fn render_example_344() {
        let ast = parse_phrases(r#"[not a `link](/foo`)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[not a <code>link](/foo</code>)</p>
"###.trim(), "Render mismatch for example 344");
    }
    #[test]
    fn render_example_345() {
        let ast = parse_phrases(r#"`<a href=\"`\">`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>&lt;a href=&quot;</code>&quot;&gt;`</p>
"###.trim(), "Render mismatch for example 345");
    }
    #[test]
    fn render_example_346() {
        let ast = parse_phrases(r#"<a href=\"`\">`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="`">`</p>
"###.trim(), "Render mismatch for example 346");
    }
    #[test]
    fn render_example_347() {
        let ast = parse_phrases(r#"`<https://foo.bar.`baz>`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>&lt;https://foo.bar.</code>baz&gt;`</p>
"###.trim(), "Render mismatch for example 347");
    }
    #[test]
    fn render_example_348() {
        let ast = parse_phrases(r#"<https://foo.bar.`baz>`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="https://foo.bar.%60baz">https://foo.bar.`baz</a>`</p>
"###.trim(), "Render mismatch for example 348");
    }
    #[test]
    fn render_example_349() {
        let ast = parse_phrases(r#"```foo``
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>```foo``</p>
"###.trim(), "Render mismatch for example 349");
    }
    #[test]
    fn render_example_350() {
        let ast = parse_phrases(r#"`foo
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>`foo</p>
"###.trim(), "Render mismatch for example 350");
    }
    #[test]
    fn render_example_351() {
        let ast = parse_phrases(r#"`foo``bar``
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>`foo<code>bar</code></p>
"###.trim(), "Render mismatch for example 351");
    }
    #[test]
    fn render_example_352() {
        let ast = parse_phrases(r#"*foo bar*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo bar</em></p>
"###.trim(), "Render mismatch for example 352");
    }
    #[test]
    fn render_example_353() {
        let ast = parse_phrases(r#"a * foo bar*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>a * foo bar*</p>
"###.trim(), "Render mismatch for example 353");
    }
    #[test]
    fn render_example_354() {
        let ast = parse_phrases(r#"a*\"foo\"*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>a*&quot;foo&quot;*</p>
"###.trim(), "Render mismatch for example 354");
    }
    #[test]
    fn render_example_355() {
        let ast = parse_phrases(r#"* a *
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>* a *</p>
"###.trim(), "Render mismatch for example 355");
    }
    #[test]
    fn render_example_356() {
        let ast = parse_phrases(r#"*$*alpha.

*£*bravo.

*€*charlie.

*𞋿*delta.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*$*alpha.</p>
<p>*£*bravo.</p>
<p>*€*charlie.</p>
<p>*𞋿*delta.</p>
"###.trim(), "Render mismatch for example 356");
    }
    #[test]
    fn render_example_357() {
        let ast = parse_phrases(r#"foo*bar*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo<em>bar</em></p>
"###.trim(), "Render mismatch for example 357");
    }
    #[test]
    fn render_example_358() {
        let ast = parse_phrases(r#"5*6*78
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>5<em>6</em>78</p>
"###.trim(), "Render mismatch for example 358");
    }
    #[test]
    fn render_example_359() {
        let ast = parse_phrases(r#"_foo bar_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo bar</em></p>
"###.trim(), "Render mismatch for example 359");
    }
    #[test]
    fn render_example_360() {
        let ast = parse_phrases(r#"_ foo bar_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>_ foo bar_</p>
"###.trim(), "Render mismatch for example 360");
    }
    #[test]
    fn render_example_361() {
        let ast = parse_phrases(r#"a_\"foo\"_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>a_&quot;foo&quot;_</p>
"###.trim(), "Render mismatch for example 361");
    }
    #[test]
    fn render_example_362() {
        let ast = parse_phrases(r#"foo_bar_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo_bar_</p>
"###.trim(), "Render mismatch for example 362");
    }
    #[test]
    fn render_example_363() {
        let ast = parse_phrases(r#"5_6_78
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>5_6_78</p>
"###.trim(), "Render mismatch for example 363");
    }
    #[test]
    fn render_example_364() {
        let ast = parse_phrases(r#"пристаням_стремятся_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>пристаням_стремятся_</p>
"###.trim(), "Render mismatch for example 364");
    }
    #[test]
    fn render_example_365() {
        let ast = parse_phrases(r#"aa_\"bb\"_cc
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>aa_&quot;bb&quot;_cc</p>
"###.trim(), "Render mismatch for example 365");
    }
    #[test]
    fn render_example_366() {
        let ast = parse_phrases(r#"foo-_(bar)_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo-<em>(bar)</em></p>
"###.trim(), "Render mismatch for example 366");
    }
    #[test]
    fn render_example_367() {
        let ast = parse_phrases(r#"_foo*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>_foo*</p>
"###.trim(), "Render mismatch for example 367");
    }
    #[test]
    fn render_example_368() {
        let ast = parse_phrases(r#"*foo bar *
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*foo bar *</p>
"###.trim(), "Render mismatch for example 368");
    }
    #[test]
    fn render_example_369() {
        let ast = parse_phrases(r#"*foo bar
*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*foo bar
*</p>
"###.trim(), "Render mismatch for example 369");
    }
    #[test]
    fn render_example_370() {
        let ast = parse_phrases(r#"*(*foo)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*(*foo)</p>
"###.trim(), "Render mismatch for example 370");
    }
    #[test]
    fn render_example_371() {
        let ast = parse_phrases(r#"*(*foo*)*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>(<em>foo</em>)</em></p>
"###.trim(), "Render mismatch for example 371");
    }
    #[test]
    fn render_example_372() {
        let ast = parse_phrases(r#"*foo*bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo</em>bar</p>
"###.trim(), "Render mismatch for example 372");
    }
    #[test]
    fn render_example_373() {
        let ast = parse_phrases(r#"_foo bar _
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>_foo bar _</p>
"###.trim(), "Render mismatch for example 373");
    }
    #[test]
    fn render_example_374() {
        let ast = parse_phrases(r#"_(_foo)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>_(_foo)</p>
"###.trim(), "Render mismatch for example 374");
    }
    #[test]
    fn render_example_375() {
        let ast = parse_phrases(r#"_(_foo_)_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>(<em>foo</em>)</em></p>
"###.trim(), "Render mismatch for example 375");
    }
    #[test]
    fn render_example_376() {
        let ast = parse_phrases(r#"_foo_bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>_foo_bar</p>
"###.trim(), "Render mismatch for example 376");
    }
    #[test]
    fn render_example_377() {
        let ast = parse_phrases(r#"_пристаням_стремятся
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>_пристаням_стремятся</p>
"###.trim(), "Render mismatch for example 377");
    }
    #[test]
    fn render_example_378() {
        let ast = parse_phrases(r#"_foo_bar_baz_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo_bar_baz</em></p>
"###.trim(), "Render mismatch for example 378");
    }
    #[test]
    fn render_example_379() {
        let ast = parse_phrases(r#"_(bar)_.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>(bar)</em>.</p>
"###.trim(), "Render mismatch for example 379");
    }
    #[test]
    fn render_example_380() {
        let ast = parse_phrases(r#"**foo bar**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo bar</strong></p>
"###.trim(), "Render mismatch for example 380");
    }
    #[test]
    fn render_example_381() {
        let ast = parse_phrases(r#"** foo bar**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>** foo bar**</p>
"###.trim(), "Render mismatch for example 381");
    }
    #[test]
    fn render_example_382() {
        let ast = parse_phrases(r#"a**\"foo\"**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>a**&quot;foo&quot;**</p>
"###.trim(), "Render mismatch for example 382");
    }
    #[test]
    fn render_example_383() {
        let ast = parse_phrases(r#"foo**bar**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo<strong>bar</strong></p>
"###.trim(), "Render mismatch for example 383");
    }
    #[test]
    fn render_example_384() {
        let ast = parse_phrases(r#"__foo bar__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo bar</strong></p>
"###.trim(), "Render mismatch for example 384");
    }
    #[test]
    fn render_example_385() {
        let ast = parse_phrases(r#"__ foo bar__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>__ foo bar__</p>
"###.trim(), "Render mismatch for example 385");
    }
    #[test]
    fn render_example_386() {
        let ast = parse_phrases(r#"__
foo bar__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>__
foo bar__</p>
"###.trim(), "Render mismatch for example 386");
    }
    #[test]
    fn render_example_387() {
        let ast = parse_phrases(r#"a__\"foo\"__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>a__&quot;foo&quot;__</p>
"###.trim(), "Render mismatch for example 387");
    }
    #[test]
    fn render_example_388() {
        let ast = parse_phrases(r#"foo__bar__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo__bar__</p>
"###.trim(), "Render mismatch for example 388");
    }
    #[test]
    fn render_example_389() {
        let ast = parse_phrases(r#"5__6__78
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>5__6__78</p>
"###.trim(), "Render mismatch for example 389");
    }
    #[test]
    fn render_example_390() {
        let ast = parse_phrases(r#"пристаням__стремятся__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>пристаням__стремятся__</p>
"###.trim(), "Render mismatch for example 390");
    }
    #[test]
    fn render_example_391() {
        let ast = parse_phrases(r#"__foo, __bar__, baz__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo, <strong>bar</strong>, baz</strong></p>
"###.trim(), "Render mismatch for example 391");
    }
    #[test]
    fn render_example_392() {
        let ast = parse_phrases(r#"foo-__(bar)__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo-<strong>(bar)</strong></p>
"###.trim(), "Render mismatch for example 392");
    }
    #[test]
    fn render_example_393() {
        let ast = parse_phrases(r#"**foo bar **
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>**foo bar **</p>
"###.trim(), "Render mismatch for example 393");
    }
    #[test]
    fn render_example_394() {
        let ast = parse_phrases(r#"**(**foo)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>**(**foo)</p>
"###.trim(), "Render mismatch for example 394");
    }
    #[test]
    fn render_example_395() {
        let ast = parse_phrases(r#"*(**foo**)*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>(<strong>foo</strong>)</em></p>
"###.trim(), "Render mismatch for example 395");
    }
    #[test]
    fn render_example_396() {
        let ast = parse_phrases(r#"**Gomphocarpus (*Gomphocarpus physocarpus*, syn.
*Asclepias physocarpa*)**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>Gomphocarpus (<em>Gomphocarpus physocarpus</em>, syn.
<em>Asclepias physocarpa</em>)</strong></p>
"###.trim(), "Render mismatch for example 396");
    }
    #[test]
    fn render_example_397() {
        let ast = parse_phrases(r#"**foo \"*bar*\" foo**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo &quot;<em>bar</em>&quot; foo</strong></p>
"###.trim(), "Render mismatch for example 397");
    }
    #[test]
    fn render_example_398() {
        let ast = parse_phrases(r#"**foo**bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo</strong>bar</p>
"###.trim(), "Render mismatch for example 398");
    }
    #[test]
    fn render_example_399() {
        let ast = parse_phrases(r#"__foo bar __
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>__foo bar __</p>
"###.trim(), "Render mismatch for example 399");
    }
    #[test]
    fn render_example_400() {
        let ast = parse_phrases(r#"__(__foo)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>__(__foo)</p>
"###.trim(), "Render mismatch for example 400");
    }
    #[test]
    fn render_example_401() {
        let ast = parse_phrases(r#"_(__foo__)_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>(<strong>foo</strong>)</em></p>
"###.trim(), "Render mismatch for example 401");
    }
    #[test]
    fn render_example_402() {
        let ast = parse_phrases(r#"__foo__bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>__foo__bar</p>
"###.trim(), "Render mismatch for example 402");
    }
    #[test]
    fn render_example_403() {
        let ast = parse_phrases(r#"__пристаням__стремятся
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>__пристаням__стремятся</p>
"###.trim(), "Render mismatch for example 403");
    }
    #[test]
    fn render_example_404() {
        let ast = parse_phrases(r#"__foo__bar__baz__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo__bar__baz</strong></p>
"###.trim(), "Render mismatch for example 404");
    }
    #[test]
    fn render_example_405() {
        let ast = parse_phrases(r#"__(bar)__.
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>(bar)</strong>.</p>
"###.trim(), "Render mismatch for example 405");
    }
    #[test]
    fn render_example_406() {
        let ast = parse_phrases(r#"*foo [bar](/url)*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo <a href="/url">bar</a></em></p>
"###.trim(), "Render mismatch for example 406");
    }
    #[test]
    fn render_example_407() {
        let ast = parse_phrases(r#"*foo
bar*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo
bar</em></p>
"###.trim(), "Render mismatch for example 407");
    }
    #[test]
    fn render_example_408() {
        let ast = parse_phrases(r#"_foo __bar__ baz_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo <strong>bar</strong> baz</em></p>
"###.trim(), "Render mismatch for example 408");
    }
    #[test]
    fn render_example_409() {
        let ast = parse_phrases(r#"_foo _bar_ baz_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo <em>bar</em> baz</em></p>
"###.trim(), "Render mismatch for example 409");
    }
    #[test]
    fn render_example_410() {
        let ast = parse_phrases(r#"__foo_ bar_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em><em>foo</em> bar</em></p>
"###.trim(), "Render mismatch for example 410");
    }
    #[test]
    fn render_example_411() {
        let ast = parse_phrases(r#"*foo *bar**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo <em>bar</em></em></p>
"###.trim(), "Render mismatch for example 411");
    }
    #[test]
    fn render_example_412() {
        let ast = parse_phrases(r#"*foo **bar** baz*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo <strong>bar</strong> baz</em></p>
"###.trim(), "Render mismatch for example 412");
    }
    #[test]
    fn render_example_413() {
        let ast = parse_phrases(r#"*foo**bar**baz*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo<strong>bar</strong>baz</em></p>
"###.trim(), "Render mismatch for example 413");
    }
    #[test]
    fn render_example_414() {
        let ast = parse_phrases(r#"*foo**bar*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo**bar</em></p>
"###.trim(), "Render mismatch for example 414");
    }
    #[test]
    fn render_example_415() {
        let ast = parse_phrases(r#"***foo** bar*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em><strong>foo</strong> bar</em></p>
"###.trim(), "Render mismatch for example 415");
    }
    #[test]
    fn render_example_416() {
        let ast = parse_phrases(r#"*foo **bar***
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo <strong>bar</strong></em></p>
"###.trim(), "Render mismatch for example 416");
    }
    #[test]
    fn render_example_417() {
        let ast = parse_phrases(r#"*foo**bar***
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo<strong>bar</strong></em></p>
"###.trim(), "Render mismatch for example 417");
    }
    #[test]
    fn render_example_418() {
        let ast = parse_phrases(r#"foo***bar***baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo<em><strong>bar</strong></em>baz</p>
"###.trim(), "Render mismatch for example 418");
    }
    #[test]
    fn render_example_419() {
        let ast = parse_phrases(r#"foo******bar*********baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo<strong><strong><strong>bar</strong></strong></strong>***baz</p>
"###.trim(), "Render mismatch for example 419");
    }
    #[test]
    fn render_example_420() {
        let ast = parse_phrases(r#"*foo **bar *baz* bim** bop*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo <strong>bar <em>baz</em> bim</strong> bop</em></p>
"###.trim(), "Render mismatch for example 420");
    }
    #[test]
    fn render_example_421() {
        let ast = parse_phrases(r#"*foo [*bar*](/url)*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo <a href="/url"><em>bar</em></a></em></p>
"###.trim(), "Render mismatch for example 421");
    }
    #[test]
    fn render_example_422() {
        let ast = parse_phrases(r#"** is not an empty emphasis
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>** is not an empty emphasis</p>
"###.trim(), "Render mismatch for example 422");
    }
    #[test]
    fn render_example_423() {
        let ast = parse_phrases(r#"**** is not an empty strong emphasis
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>**** is not an empty strong emphasis</p>
"###.trim(), "Render mismatch for example 423");
    }
    #[test]
    fn render_example_424() {
        let ast = parse_phrases(r#"**foo [bar](/url)**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo <a href="/url">bar</a></strong></p>
"###.trim(), "Render mismatch for example 424");
    }
    #[test]
    fn render_example_425() {
        let ast = parse_phrases(r#"**foo
bar**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo
bar</strong></p>
"###.trim(), "Render mismatch for example 425");
    }
    #[test]
    fn render_example_426() {
        let ast = parse_phrases(r#"__foo _bar_ baz__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo <em>bar</em> baz</strong></p>
"###.trim(), "Render mismatch for example 426");
    }
    #[test]
    fn render_example_427() {
        let ast = parse_phrases(r#"__foo __bar__ baz__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo <strong>bar</strong> baz</strong></p>
"###.trim(), "Render mismatch for example 427");
    }
    #[test]
    fn render_example_428() {
        let ast = parse_phrases(r#"____foo__ bar__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong><strong>foo</strong> bar</strong></p>
"###.trim(), "Render mismatch for example 428");
    }
    #[test]
    fn render_example_429() {
        let ast = parse_phrases(r#"**foo **bar****
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo <strong>bar</strong></strong></p>
"###.trim(), "Render mismatch for example 429");
    }
    #[test]
    fn render_example_430() {
        let ast = parse_phrases(r#"**foo *bar* baz**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo <em>bar</em> baz</strong></p>
"###.trim(), "Render mismatch for example 430");
    }
    #[test]
    fn render_example_431() {
        let ast = parse_phrases(r#"**foo*bar*baz**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo<em>bar</em>baz</strong></p>
"###.trim(), "Render mismatch for example 431");
    }
    #[test]
    fn render_example_432() {
        let ast = parse_phrases(r#"***foo* bar**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong><em>foo</em> bar</strong></p>
"###.trim(), "Render mismatch for example 432");
    }
    #[test]
    fn render_example_433() {
        let ast = parse_phrases(r#"**foo *bar***
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo <em>bar</em></strong></p>
"###.trim(), "Render mismatch for example 433");
    }
    #[test]
    fn render_example_434() {
        let ast = parse_phrases(r#"**foo *bar **baz**
bim* bop**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo <em>bar <strong>baz</strong>
bim</em> bop</strong></p>
"###.trim(), "Render mismatch for example 434");
    }
    #[test]
    fn render_example_435() {
        let ast = parse_phrases(r#"**foo [*bar*](/url)**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo <a href="/url"><em>bar</em></a></strong></p>
"###.trim(), "Render mismatch for example 435");
    }
    #[test]
    fn render_example_436() {
        let ast = parse_phrases(r#"__ is not an empty emphasis
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>__ is not an empty emphasis</p>
"###.trim(), "Render mismatch for example 436");
    }
    #[test]
    fn render_example_437() {
        let ast = parse_phrases(r#"____ is not an empty strong emphasis
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>____ is not an empty strong emphasis</p>
"###.trim(), "Render mismatch for example 437");
    }
    #[test]
    fn render_example_438() {
        let ast = parse_phrases(r#"foo ***
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo ***</p>
"###.trim(), "Render mismatch for example 438");
    }
    #[test]
    fn render_example_439() {
        let ast = parse_phrases(r#"foo *\**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <em>*</em></p>
"###.trim(), "Render mismatch for example 439");
    }
    #[test]
    fn render_example_440() {
        let ast = parse_phrases(r#"foo *_*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <em>_</em></p>
"###.trim(), "Render mismatch for example 440");
    }
    #[test]
    fn render_example_441() {
        let ast = parse_phrases(r#"foo *****
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo *****</p>
"###.trim(), "Render mismatch for example 441");
    }
    #[test]
    fn render_example_442() {
        let ast = parse_phrases(r#"foo **\***
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <strong>*</strong></p>
"###.trim(), "Render mismatch for example 442");
    }
    #[test]
    fn render_example_443() {
        let ast = parse_phrases(r#"foo **_**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <strong>_</strong></p>
"###.trim(), "Render mismatch for example 443");
    }
    #[test]
    fn render_example_444() {
        let ast = parse_phrases(r#"**foo*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*<em>foo</em></p>
"###.trim(), "Render mismatch for example 444");
    }
    #[test]
    fn render_example_445() {
        let ast = parse_phrases(r#"*foo**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo</em>*</p>
"###.trim(), "Render mismatch for example 445");
    }
    #[test]
    fn render_example_446() {
        let ast = parse_phrases(r#"***foo**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*<strong>foo</strong></p>
"###.trim(), "Render mismatch for example 446");
    }
    #[test]
    fn render_example_447() {
        let ast = parse_phrases(r#"****foo*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>***<em>foo</em></p>
"###.trim(), "Render mismatch for example 447");
    }
    #[test]
    fn render_example_448() {
        let ast = parse_phrases(r#"**foo***
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo</strong>*</p>
"###.trim(), "Render mismatch for example 448");
    }
    #[test]
    fn render_example_449() {
        let ast = parse_phrases(r#"*foo****
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo</em>***</p>
"###.trim(), "Render mismatch for example 449");
    }
    #[test]
    fn render_example_450() {
        let ast = parse_phrases(r#"foo ___
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo ___</p>
"###.trim(), "Render mismatch for example 450");
    }
    #[test]
    fn render_example_451() {
        let ast = parse_phrases(r#"foo _\__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <em>_</em></p>
"###.trim(), "Render mismatch for example 451");
    }
    #[test]
    fn render_example_452() {
        let ast = parse_phrases(r#"foo _*_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <em>*</em></p>
"###.trim(), "Render mismatch for example 452");
    }
    #[test]
    fn render_example_453() {
        let ast = parse_phrases(r#"foo _____
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo _____</p>
"###.trim(), "Render mismatch for example 453");
    }
    #[test]
    fn render_example_454() {
        let ast = parse_phrases(r#"foo __\___
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <strong>_</strong></p>
"###.trim(), "Render mismatch for example 454");
    }
    #[test]
    fn render_example_455() {
        let ast = parse_phrases(r#"foo __*__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <strong>*</strong></p>
"###.trim(), "Render mismatch for example 455");
    }
    #[test]
    fn render_example_456() {
        let ast = parse_phrases(r#"__foo_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>_<em>foo</em></p>
"###.trim(), "Render mismatch for example 456");
    }
    #[test]
    fn render_example_457() {
        let ast = parse_phrases(r#"_foo__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo</em>_</p>
"###.trim(), "Render mismatch for example 457");
    }
    #[test]
    fn render_example_458() {
        let ast = parse_phrases(r#"___foo__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>_<strong>foo</strong></p>
"###.trim(), "Render mismatch for example 458");
    }
    #[test]
    fn render_example_459() {
        let ast = parse_phrases(r#"____foo_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>___<em>foo</em></p>
"###.trim(), "Render mismatch for example 459");
    }
    #[test]
    fn render_example_460() {
        let ast = parse_phrases(r#"__foo___
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo</strong>_</p>
"###.trim(), "Render mismatch for example 460");
    }
    #[test]
    fn render_example_461() {
        let ast = parse_phrases(r#"_foo____
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo</em>___</p>
"###.trim(), "Render mismatch for example 461");
    }
    #[test]
    fn render_example_462() {
        let ast = parse_phrases(r#"**foo**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo</strong></p>
"###.trim(), "Render mismatch for example 462");
    }
    #[test]
    fn render_example_463() {
        let ast = parse_phrases(r#"*_foo_*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em><em>foo</em></em></p>
"###.trim(), "Render mismatch for example 463");
    }
    #[test]
    fn render_example_464() {
        let ast = parse_phrases(r#"__foo__
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong>foo</strong></p>
"###.trim(), "Render mismatch for example 464");
    }
    #[test]
    fn render_example_465() {
        let ast = parse_phrases(r#"_*foo*_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em><em>foo</em></em></p>
"###.trim(), "Render mismatch for example 465");
    }
    #[test]
    fn render_example_466() {
        let ast = parse_phrases(r#"****foo****
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong><strong>foo</strong></strong></p>
"###.trim(), "Render mismatch for example 466");
    }
    #[test]
    fn render_example_467() {
        let ast = parse_phrases(r#"____foo____
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong><strong>foo</strong></strong></p>
"###.trim(), "Render mismatch for example 467");
    }
    #[test]
    fn render_example_468() {
        let ast = parse_phrases(r#"******foo******
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><strong><strong><strong>foo</strong></strong></strong></p>
"###.trim(), "Render mismatch for example 468");
    }
    #[test]
    fn render_example_469() {
        let ast = parse_phrases(r#"***foo***
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em><strong>foo</strong></em></p>
"###.trim(), "Render mismatch for example 469");
    }
    #[test]
    fn render_example_470() {
        let ast = parse_phrases(r#"_____foo_____
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em><strong><strong>foo</strong></strong></em></p>
"###.trim(), "Render mismatch for example 470");
    }
    #[test]
    fn render_example_471() {
        let ast = parse_phrases(r#"*foo _bar* baz_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo _bar</em> baz_</p>
"###.trim(), "Render mismatch for example 471");
    }
    #[test]
    fn render_example_472() {
        let ast = parse_phrases(r#"*foo __bar *baz bim__ bam*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo <strong>bar *baz bim</strong> bam</em></p>
"###.trim(), "Render mismatch for example 472");
    }
    #[test]
    fn render_example_473() {
        let ast = parse_phrases(r#"**foo **bar baz**
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>**foo <strong>bar baz</strong></p>
"###.trim(), "Render mismatch for example 473");
    }
    #[test]
    fn render_example_474() {
        let ast = parse_phrases(r#"*foo *bar baz*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*foo <em>bar baz</em></p>
"###.trim(), "Render mismatch for example 474");
    }
    #[test]
    fn render_example_475() {
        let ast = parse_phrases(r#"*[bar*](/url)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*<a href="/url">bar*</a></p>
"###.trim(), "Render mismatch for example 475");
    }
    #[test]
    fn render_example_476() {
        let ast = parse_phrases(r#"_foo [bar_](/url)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>_foo <a href="/url">bar_</a></p>
"###.trim(), "Render mismatch for example 476");
    }
    #[test]
    fn render_example_477() {
        let ast = parse_phrases(r#"*<img src=\"foo\" title=\"*\"/>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*<img src="foo" title="*"/></p>
"###.trim(), "Render mismatch for example 477");
    }
    #[test]
    fn render_example_478() {
        let ast = parse_phrases(r#"**<a href=\"**\">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>**<a href="**"></p>
"###.trim(), "Render mismatch for example 478");
    }
    #[test]
    fn render_example_479() {
        let ast = parse_phrases(r#"__<a href=\"__\">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>__<a href="__"></p>
"###.trim(), "Render mismatch for example 479");
    }
    #[test]
    fn render_example_480() {
        let ast = parse_phrases(r#"*a `*`*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>a <code>*</code></em></p>
"###.trim(), "Render mismatch for example 480");
    }
    #[test]
    fn render_example_481() {
        let ast = parse_phrases(r#"_a `_`_
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>a <code>_</code></em></p>
"###.trim(), "Render mismatch for example 481");
    }
    #[test]
    fn render_example_482() {
        let ast = parse_phrases(r#"**a<https://foo.bar/?q=**>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>**a<a href="https://foo.bar/?q=**">https://foo.bar/?q=**</a></p>
"###.trim(), "Render mismatch for example 482");
    }
    #[test]
    fn render_example_483() {
        let ast = parse_phrases(r#"__a<https://foo.bar/?q=__>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>__a<a href="https://foo.bar/?q=__">https://foo.bar/?q=__</a></p>
"###.trim(), "Render mismatch for example 483");
    }
    #[test]
    fn render_example_484() {
        let ast = parse_phrases(r#"[link](/uri \"title\")
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri" title="title">link</a></p>
"###.trim(), "Render mismatch for example 484");
    }
    #[test]
    fn render_example_485() {
        let ast = parse_phrases(r#"[link](/uri)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri">link</a></p>
"###.trim(), "Render mismatch for example 485");
    }
    #[test]
    fn render_example_486() {
        let ast = parse_phrases(r#"[](./target.md)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="./target.md"></a></p>
"###.trim(), "Render mismatch for example 486");
    }
    #[test]
    fn render_example_487() {
        let ast = parse_phrases(r#"[link]()
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="">link</a></p>
"###.trim(), "Render mismatch for example 487");
    }
    #[test]
    fn render_example_488() {
        let ast = parse_phrases(r#"[link](<>)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="">link</a></p>
"###.trim(), "Render mismatch for example 488");
    }
    #[test]
    fn render_example_489() {
        let ast = parse_phrases(r#"[]()
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href=""></a></p>
"###.trim(), "Render mismatch for example 489");
    }
    #[test]
    fn render_example_490() {
        let ast = parse_phrases(r#"[link](/my uri)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[link](/my uri)</p>
"###.trim(), "Render mismatch for example 490");
    }
    #[test]
    fn render_example_491() {
        let ast = parse_phrases(r#"[link](</my uri>)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/my%20uri">link</a></p>
"###.trim(), "Render mismatch for example 491");
    }
    #[test]
    fn render_example_492() {
        let ast = parse_phrases(r#"[link](foo
bar)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[link](foo
bar)</p>
"###.trim(), "Render mismatch for example 492");
    }
    #[test]
    fn render_example_493() {
        let ast = parse_phrases(r#"[link](<foo
bar>)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[link](<foo
bar>)</p>
"###.trim(), "Render mismatch for example 493");
    }
    #[test]
    fn render_example_494() {
        let ast = parse_phrases(r#"[a](<b)c>)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="b)c">a</a></p>
"###.trim(), "Render mismatch for example 494");
    }
    #[test]
    fn render_example_495() {
        let ast = parse_phrases(r#"[link](<foo\>)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[link](&lt;foo&gt;)</p>
"###.trim(), "Render mismatch for example 495");
    }
    #[test]
    fn render_example_496() {
        let ast = parse_phrases(r#"[a](<b)c
[a](<b)c>
[a](<b>c)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[a](&lt;b)c
[a](&lt;b)c&gt;
[a](<b>c)</p>
"###.trim(), "Render mismatch for example 496");
    }
    #[test]
    fn render_example_497() {
        let ast = parse_phrases(r#"[link](\(foo\))
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="(foo)">link</a></p>
"###.trim(), "Render mismatch for example 497");
    }
    #[test]
    fn render_example_498() {
        let ast = parse_phrases(r#"[link](foo(and(bar)))
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="foo(and(bar))">link</a></p>
"###.trim(), "Render mismatch for example 498");
    }
    #[test]
    fn render_example_499() {
        let ast = parse_phrases(r#"[link](foo(and(bar))
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[link](foo(and(bar))</p>
"###.trim(), "Render mismatch for example 499");
    }
    #[test]
    fn render_example_500() {
        let ast = parse_phrases(r#"[link](foo\(and\(bar\))
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="foo(and(bar)">link</a></p>
"###.trim(), "Render mismatch for example 500");
    }
    #[test]
    fn render_example_501() {
        let ast = parse_phrases(r#"[link](<foo(and(bar)>)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="foo(and(bar)">link</a></p>
"###.trim(), "Render mismatch for example 501");
    }
    #[test]
    fn render_example_502() {
        let ast = parse_phrases(r#"[link](foo\)\:)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="foo):">link</a></p>
"###.trim(), "Render mismatch for example 502");
    }
    #[test]
    fn render_example_503() {
        let ast = parse_phrases(r#"[link](#fragment)

[link](https://example.com#fragment)

[link](https://example.com?foo=3#frag)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="#fragment">link</a></p>
<p><a href="https://example.com#fragment">link</a></p>
<p><a href="https://example.com?foo=3#frag">link</a></p>
"###.trim(), "Render mismatch for example 503");
    }
    #[test]
    fn render_example_504() {
        let ast = parse_phrases(r#"[link](foo\bar)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="foo%5Cbar">link</a></p>
"###.trim(), "Render mismatch for example 504");
    }
    #[test]
    fn render_example_505() {
        let ast = parse_phrases(r#"[link](foo%20b&auml;)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="foo%20b%C3%A4">link</a></p>
"###.trim(), "Render mismatch for example 505");
    }
    #[test]
    fn render_example_506() {
        let ast = parse_phrases(r#"[link](\"title\")
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="%22title%22">link</a></p>
"###.trim(), "Render mismatch for example 506");
    }
    #[test]
    fn render_example_507() {
        let ast = parse_phrases(r#"[link](/url \"title\")
[link](/url 'title')
[link](/url (title))
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title">link</a>
<a href="/url" title="title">link</a>
<a href="/url" title="title">link</a></p>
"###.trim(), "Render mismatch for example 507");
    }
    #[test]
    fn render_example_508() {
        let ast = parse_phrases(r#"[link](/url \"title \\"&quot;\")
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title &quot;&quot;">link</a></p>
"###.trim(), "Render mismatch for example 508");
    }
    #[test]
    fn render_example_509() {
        let ast = parse_phrases(r#"[link](/url \"title\")
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url%C2%A0%22title%22">link</a></p>
"###.trim(), "Render mismatch for example 509");
    }
    #[test]
    fn render_example_510() {
        let ast = parse_phrases(r#"[link](/url \"title \"and\" title\")
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[link](/url &quot;title &quot;and&quot; title&quot;)</p>
"###.trim(), "Render mismatch for example 510");
    }
    #[test]
    fn render_example_511() {
        let ast = parse_phrases(r#"[link](/url 'title \"and\" title')
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title &quot;and&quot; title">link</a></p>
"###.trim(), "Render mismatch for example 511");
    }
    #[test]
    fn render_example_512() {
        let ast = parse_phrases(r#"[link](   /uri
  \"title\"  )
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri" title="title">link</a></p>
"###.trim(), "Render mismatch for example 512");
    }
    #[test]
    fn render_example_513() {
        let ast = parse_phrases(r#"[link] (/uri)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[link] (/uri)</p>
"###.trim(), "Render mismatch for example 513");
    }
    #[test]
    fn render_example_514() {
        let ast = parse_phrases(r#"[link [foo [bar]]](/uri)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri">link [foo [bar]]</a></p>
"###.trim(), "Render mismatch for example 514");
    }
    #[test]
    fn render_example_515() {
        let ast = parse_phrases(r#"[link] bar](/uri)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[link] bar](/uri)</p>
"###.trim(), "Render mismatch for example 515");
    }
    #[test]
    fn render_example_516() {
        let ast = parse_phrases(r#"[link [bar](/uri)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[link <a href="/uri">bar</a></p>
"###.trim(), "Render mismatch for example 516");
    }
    #[test]
    fn render_example_517() {
        let ast = parse_phrases(r#"[link \[bar](/uri)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri">link [bar</a></p>
"###.trim(), "Render mismatch for example 517");
    }
    #[test]
    fn render_example_518() {
        let ast = parse_phrases(r#"[link *foo **bar** `#`*](/uri)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri">link <em>foo <strong>bar</strong> <code>#</code></em></a></p>
"###.trim(), "Render mismatch for example 518");
    }
    #[test]
    fn render_example_519() {
        let ast = parse_phrases(r#"[![moon](moon.jpg)](/uri)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri"><img src="moon.jpg" alt="moon" /></a></p>
"###.trim(), "Render mismatch for example 519");
    }
    #[test]
    fn render_example_520() {
        let ast = parse_phrases(r#"[foo [bar](/uri)](/uri)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo <a href="/uri">bar</a>](/uri)</p>
"###.trim(), "Render mismatch for example 520");
    }
    #[test]
    fn render_example_521() {
        let ast = parse_phrases(r#"[foo *[bar [baz](/uri)](/uri)*](/uri)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo <em>[bar <a href="/uri">baz</a>](/uri)</em>](/uri)</p>
"###.trim(), "Render mismatch for example 521");
    }
    #[test]
    fn render_example_522() {
        let ast = parse_phrases(r#"![[[foo](uri1)](uri2)](uri3)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="uri3" alt="[foo](uri2)" /></p>
"###.trim(), "Render mismatch for example 522");
    }
    #[test]
    fn render_example_523() {
        let ast = parse_phrases(r#"*[foo*](/uri)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*<a href="/uri">foo*</a></p>
"###.trim(), "Render mismatch for example 523");
    }
    #[test]
    fn render_example_524() {
        let ast = parse_phrases(r#"[foo *bar](baz*)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="baz*">foo *bar</a></p>
"###.trim(), "Render mismatch for example 524");
    }
    #[test]
    fn render_example_525() {
        let ast = parse_phrases(r#"*foo [bar* baz]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo [bar</em> baz]</p>
"###.trim(), "Render mismatch for example 525");
    }
    #[test]
    fn render_example_526() {
        let ast = parse_phrases(r#"[foo <bar attr=\"](baz)\">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo <bar attr="](baz)"></p>
"###.trim(), "Render mismatch for example 526");
    }
    #[test]
    fn render_example_527() {
        let ast = parse_phrases(r#"[foo`](/uri)`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo<code>](/uri)</code></p>
"###.trim(), "Render mismatch for example 527");
    }
    #[test]
    fn render_example_528() {
        let ast = parse_phrases(r#"[foo<https://example.com/?search=](uri)>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo<a href="https://example.com/?search=%5D(uri)">https://example.com/?search=](uri)</a></p>
"###.trim(), "Render mismatch for example 528");
    }
    #[test]
    fn render_example_529() {
        let ast = parse_phrases(r#"[foo][bar]

[bar]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title">foo</a></p>
"###.trim(), "Render mismatch for example 529");
    }
    #[test]
    fn render_example_530() {
        let ast = parse_phrases(r#"[link [foo [bar]]][ref]

[ref]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri">link [foo [bar]]</a></p>
"###.trim(), "Render mismatch for example 530");
    }
    #[test]
    fn render_example_531() {
        let ast = parse_phrases(r#"[link \[bar][ref]

[ref]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri">link [bar</a></p>
"###.trim(), "Render mismatch for example 531");
    }
    #[test]
    fn render_example_532() {
        let ast = parse_phrases(r#"[link *foo **bar** `#`*][ref]

[ref]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri">link <em>foo <strong>bar</strong> <code>#</code></em></a></p>
"###.trim(), "Render mismatch for example 532");
    }
    #[test]
    fn render_example_533() {
        let ast = parse_phrases(r#"[![moon](moon.jpg)][ref]

[ref]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri"><img src="moon.jpg" alt="moon" /></a></p>
"###.trim(), "Render mismatch for example 533");
    }
    #[test]
    fn render_example_534() {
        let ast = parse_phrases(r#"[foo [bar](/uri)][ref]

[ref]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo <a href="/uri">bar</a>]<a href="/uri">ref</a></p>
"###.trim(), "Render mismatch for example 534");
    }
    #[test]
    fn render_example_535() {
        let ast = parse_phrases(r#"[foo *bar [baz][ref]*][ref]

[ref]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo <em>bar <a href="/uri">baz</a></em>]<a href="/uri">ref</a></p>
"###.trim(), "Render mismatch for example 535");
    }
    #[test]
    fn render_example_536() {
        let ast = parse_phrases(r#"*[foo*][ref]

[ref]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*<a href="/uri">foo*</a></p>
"###.trim(), "Render mismatch for example 536");
    }
    #[test]
    fn render_example_537() {
        let ast = parse_phrases(r#"[foo *bar][ref]*

[ref]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri">foo *bar</a>*</p>
"###.trim(), "Render mismatch for example 537");
    }
    #[test]
    fn render_example_538() {
        let ast = parse_phrases(r#"[foo <bar attr=\"][ref]\">

[ref]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo <bar attr="][ref]"></p>
"###.trim(), "Render mismatch for example 538");
    }
    #[test]
    fn render_example_539() {
        let ast = parse_phrases(r#"[foo`][ref]`

[ref]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo<code>][ref]</code></p>
"###.trim(), "Render mismatch for example 539");
    }
    #[test]
    fn render_example_540() {
        let ast = parse_phrases(r#"[foo<https://example.com/?search=][ref]>

[ref]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo<a href="https://example.com/?search=%5D%5Bref%5D">https://example.com/?search=][ref]</a></p>
"###.trim(), "Render mismatch for example 540");
    }
    #[test]
    fn render_example_541() {
        let ast = parse_phrases(r#"[foo][BaR]

[bar]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title">foo</a></p>
"###.trim(), "Render mismatch for example 541");
    }
    #[test]
    fn render_example_542() {
        let ast = parse_phrases(r#"[ẞ]

[SS]: /url
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url">ẞ</a></p>
"###.trim(), "Render mismatch for example 542");
    }
    #[test]
    fn render_example_543() {
        let ast = parse_phrases(r#"[Foo
  bar]: /url

[Baz][Foo bar]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url">Baz</a></p>
"###.trim(), "Render mismatch for example 543");
    }
    #[test]
    fn render_example_544() {
        let ast = parse_phrases(r#"[foo] [bar]

[bar]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo] <a href="/url" title="title">bar</a></p>
"###.trim(), "Render mismatch for example 544");
    }
    #[test]
    fn render_example_545() {
        let ast = parse_phrases(r#"[foo]
[bar]

[bar]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo]
<a href="/url" title="title">bar</a></p>
"###.trim(), "Render mismatch for example 545");
    }
    #[test]
    fn render_example_546() {
        let ast = parse_phrases(r#"[foo]: /url1

[foo]: /url2

[bar][foo]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url1">bar</a></p>
"###.trim(), "Render mismatch for example 546");
    }
    #[test]
    fn render_example_547() {
        let ast = parse_phrases(r#"[bar][foo\!]

[foo!]: /url
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[bar][foo!]</p>
"###.trim(), "Render mismatch for example 547");
    }
    #[test]
    fn render_example_548() {
        let ast = parse_phrases(r#"[foo][ref[]

[ref[]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo][ref[]</p>
<p>[ref[]: /uri</p>
"###.trim(), "Render mismatch for example 548");
    }
    #[test]
    fn render_example_549() {
        let ast = parse_phrases(r#"[foo][ref[bar]]

[ref[bar]]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo][ref[bar]]</p>
<p>[ref[bar]]: /uri</p>
"###.trim(), "Render mismatch for example 549");
    }
    #[test]
    fn render_example_550() {
        let ast = parse_phrases(r#"[[[foo]]]

[[[foo]]]: /url
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[[[foo]]]</p>
<p>[[[foo]]]: /url</p>
"###.trim(), "Render mismatch for example 550");
    }
    #[test]
    fn render_example_551() {
        let ast = parse_phrases(r#"[foo][ref\[]

[ref\[]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri">foo</a></p>
"###.trim(), "Render mismatch for example 551");
    }
    #[test]
    fn render_example_552() {
        let ast = parse_phrases(r#"[bar\\]: /uri

[bar\\]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/uri">bar\</a></p>
"###.trim(), "Render mismatch for example 552");
    }
    #[test]
    fn render_example_553() {
        let ast = parse_phrases(r#"[]

[]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[]</p>
<p>[]: /uri</p>
"###.trim(), "Render mismatch for example 553");
    }
    #[test]
    fn render_example_554() {
        let ast = parse_phrases(r#"[
 ]

[
 ]: /uri
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[
]</p>
<p>[
]: /uri</p>
"###.trim(), "Render mismatch for example 554");
    }
    #[test]
    fn render_example_555() {
        let ast = parse_phrases(r#"[foo][]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title">foo</a></p>
"###.trim(), "Render mismatch for example 555");
    }
    #[test]
    fn render_example_556() {
        let ast = parse_phrases(r#"[*foo* bar][]

[*foo* bar]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title"><em>foo</em> bar</a></p>
"###.trim(), "Render mismatch for example 556");
    }
    #[test]
    fn render_example_557() {
        let ast = parse_phrases(r#"[Foo][]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title">Foo</a></p>
"###.trim(), "Render mismatch for example 557");
    }
    #[test]
    fn render_example_558() {
        let ast = parse_phrases(r#"[foo] 
[]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title">foo</a>
[]</p>
"###.trim(), "Render mismatch for example 558");
    }
    #[test]
    fn render_example_559() {
        let ast = parse_phrases(r#"[foo]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title">foo</a></p>
"###.trim(), "Render mismatch for example 559");
    }
    #[test]
    fn render_example_560() {
        let ast = parse_phrases(r#"[*foo* bar]

[*foo* bar]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title"><em>foo</em> bar</a></p>
"###.trim(), "Render mismatch for example 560");
    }
    #[test]
    fn render_example_561() {
        let ast = parse_phrases(r#"[[*foo* bar]]

[*foo* bar]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[<a href="/url" title="title"><em>foo</em> bar</a>]</p>
"###.trim(), "Render mismatch for example 561");
    }
    #[test]
    fn render_example_562() {
        let ast = parse_phrases(r#"[[bar [foo]

[foo]: /url
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[[bar <a href="/url">foo</a></p>
"###.trim(), "Render mismatch for example 562");
    }
    #[test]
    fn render_example_563() {
        let ast = parse_phrases(r#"[Foo]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url" title="title">Foo</a></p>
"###.trim(), "Render mismatch for example 563");
    }
    #[test]
    fn render_example_564() {
        let ast = parse_phrases(r#"[foo] bar

[foo]: /url
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url">foo</a> bar</p>
"###.trim(), "Render mismatch for example 564");
    }
    #[test]
    fn render_example_565() {
        let ast = parse_phrases(r#"\[foo]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo]</p>
"###.trim(), "Render mismatch for example 565");
    }
    #[test]
    fn render_example_566() {
        let ast = parse_phrases(r#"[foo*]: /url

*[foo*]
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>*<a href="/url">foo*</a></p>
"###.trim(), "Render mismatch for example 566");
    }
    #[test]
    fn render_example_567() {
        let ast = parse_phrases(r#"[foo][bar]

[foo]: /url1
[bar]: /url2
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url2">foo</a></p>
"###.trim(), "Render mismatch for example 567");
    }
    #[test]
    fn render_example_568() {
        let ast = parse_phrases(r#"[foo][]

[foo]: /url1
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url1">foo</a></p>
"###.trim(), "Render mismatch for example 568");
    }
    #[test]
    fn render_example_569() {
        let ast = parse_phrases(r#"[foo]()

[foo]: /url1
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="">foo</a></p>
"###.trim(), "Render mismatch for example 569");
    }
    #[test]
    fn render_example_570() {
        let ast = parse_phrases(r#"[foo](not a link)

[foo]: /url1
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url1">foo</a>(not a link)</p>
"###.trim(), "Render mismatch for example 570");
    }
    #[test]
    fn render_example_571() {
        let ast = parse_phrases(r#"[foo][bar][baz]

[baz]: /url
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo]<a href="/url">bar</a></p>
"###.trim(), "Render mismatch for example 571");
    }
    #[test]
    fn render_example_572() {
        let ast = parse_phrases(r#"[foo][bar][baz]

[baz]: /url1
[bar]: /url2
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="/url2">foo</a><a href="/url1">baz</a></p>
"###.trim(), "Render mismatch for example 572");
    }
    #[test]
    fn render_example_573() {
        let ast = parse_phrases(r#"[foo][bar][baz]

[baz]: /url1
[foo]: /url2
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>[foo]<a href="/url1">bar</a></p>
"###.trim(), "Render mismatch for example 573");
    }
    #[test]
    fn render_example_574() {
        let ast = parse_phrases(r#"![foo](/url \"title\")
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url" alt="foo" title="title" /></p>
"###.trim(), "Render mismatch for example 574");
    }
    #[test]
    fn render_example_575() {
        let ast = parse_phrases(r#"![foo *bar*]

[foo *bar*]: train.jpg \"train & tracks\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="train.jpg" alt="foo bar" title="train &amp; tracks" /></p>
"###.trim(), "Render mismatch for example 575");
    }
    #[test]
    fn render_example_576() {
        let ast = parse_phrases(r#"![foo ![bar](/url)](/url2)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url2" alt="foo bar" /></p>
"###.trim(), "Render mismatch for example 576");
    }
    #[test]
    fn render_example_577() {
        let ast = parse_phrases(r#"![foo [bar](/url)](/url2)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url2" alt="foo bar" /></p>
"###.trim(), "Render mismatch for example 577");
    }
    #[test]
    fn render_example_578() {
        let ast = parse_phrases(r#"![foo *bar*][]

[foo *bar*]: train.jpg \"train & tracks\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="train.jpg" alt="foo bar" title="train &amp; tracks" /></p>
"###.trim(), "Render mismatch for example 578");
    }
    #[test]
    fn render_example_579() {
        let ast = parse_phrases(r#"![foo *bar*][foobar]

[FOOBAR]: train.jpg \"train & tracks\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="train.jpg" alt="foo bar" title="train &amp; tracks" /></p>
"###.trim(), "Render mismatch for example 579");
    }
    #[test]
    fn render_example_580() {
        let ast = parse_phrases(r#"![foo](train.jpg)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="train.jpg" alt="foo" /></p>
"###.trim(), "Render mismatch for example 580");
    }
    #[test]
    fn render_example_581() {
        let ast = parse_phrases(r#"My ![foo bar](/path/to/train.jpg  \"title\"   )
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>My <img src="/path/to/train.jpg" alt="foo bar" title="title" /></p>
"###.trim(), "Render mismatch for example 581");
    }
    #[test]
    fn render_example_582() {
        let ast = parse_phrases(r#"![foo](<url>)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="url" alt="foo" /></p>
"###.trim(), "Render mismatch for example 582");
    }
    #[test]
    fn render_example_583() {
        let ast = parse_phrases(r#"![](/url)
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url" alt="" /></p>
"###.trim(), "Render mismatch for example 583");
    }
    #[test]
    fn render_example_584() {
        let ast = parse_phrases(r#"![foo][bar]

[bar]: /url
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url" alt="foo" /></p>
"###.trim(), "Render mismatch for example 584");
    }
    #[test]
    fn render_example_585() {
        let ast = parse_phrases(r#"![foo][bar]

[BAR]: /url
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url" alt="foo" /></p>
"###.trim(), "Render mismatch for example 585");
    }
    #[test]
    fn render_example_586() {
        let ast = parse_phrases(r#"![foo][]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url" alt="foo" title="title" /></p>
"###.trim(), "Render mismatch for example 586");
    }
    #[test]
    fn render_example_587() {
        let ast = parse_phrases(r#"![*foo* bar][]

[*foo* bar]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url" alt="foo bar" title="title" /></p>
"###.trim(), "Render mismatch for example 587");
    }
    #[test]
    fn render_example_588() {
        let ast = parse_phrases(r#"![Foo][]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url" alt="Foo" title="title" /></p>
"###.trim(), "Render mismatch for example 588");
    }
    #[test]
    fn render_example_589() {
        let ast = parse_phrases(r#"![foo] 
[]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url" alt="foo" title="title" />
[]</p>
"###.trim(), "Render mismatch for example 589");
    }
    #[test]
    fn render_example_590() {
        let ast = parse_phrases(r#"![foo]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url" alt="foo" title="title" /></p>
"###.trim(), "Render mismatch for example 590");
    }
    #[test]
    fn render_example_591() {
        let ast = parse_phrases(r#"![*foo* bar]

[*foo* bar]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url" alt="foo bar" title="title" /></p>
"###.trim(), "Render mismatch for example 591");
    }
    #[test]
    fn render_example_592() {
        let ast = parse_phrases(r#"![[foo]]

[[foo]]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>![[foo]]</p>
<p>[[foo]]: /url &quot;title&quot;</p>
"###.trim(), "Render mismatch for example 592");
    }
    #[test]
    fn render_example_593() {
        let ast = parse_phrases(r#"![Foo]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><img src="/url" alt="Foo" title="title" /></p>
"###.trim(), "Render mismatch for example 593");
    }
    #[test]
    fn render_example_594() {
        let ast = parse_phrases(r#"!\[foo]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>![foo]</p>
"###.trim(), "Render mismatch for example 594");
    }
    #[test]
    fn render_example_595() {
        let ast = parse_phrases(r#"\![foo]

[foo]: /url \"title\"
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>!<a href="/url" title="title">foo</a></p>
"###.trim(), "Render mismatch for example 595");
    }
    #[test]
    fn render_example_596() {
        let ast = parse_phrases(r#"<http://foo.bar.baz>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="http://foo.bar.baz">http://foo.bar.baz</a></p>
"###.trim(), "Render mismatch for example 596");
    }
    #[test]
    fn render_example_597() {
        let ast = parse_phrases(r#"<https://foo.bar.baz/test?q=hello&id=22&boolean>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="https://foo.bar.baz/test?q=hello&amp;id=22&amp;boolean">https://foo.bar.baz/test?q=hello&amp;id=22&amp;boolean</a></p>
"###.trim(), "Render mismatch for example 597");
    }
    #[test]
    fn render_example_598() {
        let ast = parse_phrases(r#"<irc://foo.bar:2233/baz>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="irc://foo.bar:2233/baz">irc://foo.bar:2233/baz</a></p>
"###.trim(), "Render mismatch for example 598");
    }
    #[test]
    fn render_example_599() {
        let ast = parse_phrases(r#"<MAILTO:FOO@BAR.BAZ>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="MAILTO:FOO@BAR.BAZ">MAILTO:FOO@BAR.BAZ</a></p>
"###.trim(), "Render mismatch for example 599");
    }
    #[test]
    fn render_example_600() {
        let ast = parse_phrases(r#"<a+b+c:d>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="a+b+c:d">a+b+c:d</a></p>
"###.trim(), "Render mismatch for example 600");
    }
    #[test]
    fn render_example_601() {
        let ast = parse_phrases(r#"<made-up-scheme://foo,bar>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="made-up-scheme://foo,bar">made-up-scheme://foo,bar</a></p>
"###.trim(), "Render mismatch for example 601");
    }
    #[test]
    fn render_example_602() {
        let ast = parse_phrases(r#"<https://../>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="https://../">https://../</a></p>
"###.trim(), "Render mismatch for example 602");
    }
    #[test]
    fn render_example_603() {
        let ast = parse_phrases(r#"<localhost:5001/foo>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="localhost:5001/foo">localhost:5001/foo</a></p>
"###.trim(), "Render mismatch for example 603");
    }
    #[test]
    fn render_example_604() {
        let ast = parse_phrases(r#"<https://foo.bar/baz bim>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt;https://foo.bar/baz bim&gt;</p>
"###.trim(), "Render mismatch for example 604");
    }
    #[test]
    fn render_example_605() {
        let ast = parse_phrases(r#"<https://example.com/\[\>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="https://example.com/%5C%5B%5C">https://example.com/\[\</a></p>
"###.trim(), "Render mismatch for example 605");
    }
    #[test]
    fn render_example_606() {
        let ast = parse_phrases(r#"<foo@bar.example.com>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="mailto:foo@bar.example.com">foo@bar.example.com</a></p>
"###.trim(), "Render mismatch for example 606");
    }
    #[test]
    fn render_example_607() {
        let ast = parse_phrases(r#"<foo+special@Bar.baz-bar0.com>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="mailto:foo+special@Bar.baz-bar0.com">foo+special@Bar.baz-bar0.com</a></p>
"###.trim(), "Render mismatch for example 607");
    }
    #[test]
    fn render_example_608() {
        let ast = parse_phrases(r#"<foo\+@bar.example.com>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt;foo+@bar.example.com&gt;</p>
"###.trim(), "Render mismatch for example 608");
    }
    #[test]
    fn render_example_609() {
        let ast = parse_phrases(r#"<>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt;&gt;</p>
"###.trim(), "Render mismatch for example 609");
    }
    #[test]
    fn render_example_610() {
        let ast = parse_phrases(r#"< https://foo.bar >
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt; https://foo.bar &gt;</p>
"###.trim(), "Render mismatch for example 610");
    }
    #[test]
    fn render_example_611() {
        let ast = parse_phrases(r#"<m:abc>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt;m:abc&gt;</p>
"###.trim(), "Render mismatch for example 611");
    }
    #[test]
    fn render_example_612() {
        let ast = parse_phrases(r#"<foo.bar.baz>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt;foo.bar.baz&gt;</p>
"###.trim(), "Render mismatch for example 612");
    }
    #[test]
    fn render_example_613() {
        let ast = parse_phrases(r#"https://example.com
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>https://example.com</p>
"###.trim(), "Render mismatch for example 613");
    }
    #[test]
    fn render_example_614() {
        let ast = parse_phrases(r#"foo@bar.example.com
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo@bar.example.com</p>
"###.trim(), "Render mismatch for example 614");
    }
    #[test]
    fn render_example_615() {
        let ast = parse_phrases(r#"<a><bab><c2c>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a><bab><c2c></p>
"###.trim(), "Render mismatch for example 615");
    }
    #[test]
    fn render_example_616() {
        let ast = parse_phrases(r#"<a/><b2/>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a/><b2/></p>
"###.trim(), "Render mismatch for example 616");
    }
    #[test]
    fn render_example_617() {
        let ast = parse_phrases(r#"<a  /><b2
data=\"foo\" >
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a  /><b2
data="foo" ></p>
"###.trim(), "Render mismatch for example 617");
    }
    #[test]
    fn render_example_618() {
        let ast = parse_phrases(r#"<a foo=\"bar\" bam = 'baz <em>\"</em>'
_boolean zoop:33=zoop:33 />
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a foo="bar" bam = 'baz <em>"</em>'
_boolean zoop:33=zoop:33 /></p>
"###.trim(), "Render mismatch for example 618");
    }
    #[test]
    fn render_example_619() {
        let ast = parse_phrases(r#"Foo <responsive-image src=\"foo.jpg\" />
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo <responsive-image src="foo.jpg" /></p>
"###.trim(), "Render mismatch for example 619");
    }
    #[test]
    fn render_example_620() {
        let ast = parse_phrases(r#"<33> <__>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt;33&gt; &lt;__&gt;</p>
"###.trim(), "Render mismatch for example 620");
    }
    #[test]
    fn render_example_621() {
        let ast = parse_phrases(r#"<a h*#ref=\"hi\">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt;a h*#ref=&quot;hi&quot;&gt;</p>
"###.trim(), "Render mismatch for example 621");
    }
    #[test]
    fn render_example_622() {
        let ast = parse_phrases(r#"<a href=\"hi'> <a href=hi'>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt;a href=&quot;hi'&gt; &lt;a href=hi'&gt;</p>
"###.trim(), "Render mismatch for example 622");
    }
    #[test]
    fn render_example_623() {
        let ast = parse_phrases(r#"< a><
foo><bar/ >
<foo bar=baz
bim!bop />
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt; a&gt;&lt;
foo&gt;&lt;bar/ &gt;
&lt;foo bar=baz
bim!bop /&gt;</p>
"###.trim(), "Render mismatch for example 623");
    }
    #[test]
    fn render_example_624() {
        let ast = parse_phrases(r#"<a href='bar'title=title>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt;a href='bar'title=title&gt;</p>
"###.trim(), "Render mismatch for example 624");
    }
    #[test]
    fn render_example_625() {
        let ast = parse_phrases(r#"</a></foo >
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p></a></foo ></p>
"###.trim(), "Render mismatch for example 625");
    }
    #[test]
    fn render_example_626() {
        let ast = parse_phrases(r#"</a href=\"foo\">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt;/a href=&quot;foo&quot;&gt;</p>
"###.trim(), "Render mismatch for example 626");
    }
    #[test]
    fn render_example_627() {
        let ast = parse_phrases(r#"foo <!-- this is a --
comment - with hyphens -->
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <!-- this is a --
comment - with hyphens --></p>
"###.trim(), "Render mismatch for example 627");
    }
    #[test]
    fn render_example_628() {
        let ast = parse_phrases(r#"foo <!--> foo -->

foo <!---> foo -->
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <!--> foo --&gt;</p>
<p>foo <!---> foo --&gt;</p>
"###.trim(), "Render mismatch for example 628");
    }
    #[test]
    fn render_example_629() {
        let ast = parse_phrases(r#"foo <?php echo $a; ?>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <?php echo $a; ?></p>
"###.trim(), "Render mismatch for example 629");
    }
    #[test]
    fn render_example_630() {
        let ast = parse_phrases(r#"foo <!ELEMENT br EMPTY>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <!ELEMENT br EMPTY></p>
"###.trim(), "Render mismatch for example 630");
    }
    #[test]
    fn render_example_631() {
        let ast = parse_phrases(r#"foo <![CDATA[>&<]]>
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <![CDATA[>&<]]></p>
"###.trim(), "Render mismatch for example 631");
    }
    #[test]
    fn render_example_632() {
        let ast = parse_phrases(r#"foo <a href=\"&ouml;\">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <a href="&ouml;"></p>
"###.trim(), "Render mismatch for example 632");
    }
    #[test]
    fn render_example_633() {
        let ast = parse_phrases(r#"foo <a href=\"\*\">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo <a href="\*"></p>
"###.trim(), "Render mismatch for example 633");
    }
    #[test]
    fn render_example_634() {
        let ast = parse_phrases(r#"<a href=\"\\"\">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt;a href=&quot;&quot;&quot;&gt;</p>
"###.trim(), "Render mismatch for example 634");
    }
    #[test]
    fn render_example_635() {
        let ast = parse_phrases(r#"<a
> quoted text
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>&lt;a</p>
<blockquote>
<p>quoted text</p>
</blockquote>
"###.trim(), "Render mismatch for example 635");
    }
    #[test]
    fn render_example_636() {
        let ast = parse_phrases(r#"foo  
baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo<br />
baz</p>
"###.trim(), "Render mismatch for example 636");
    }
    #[test]
    fn render_example_637() {
        let ast = parse_phrases(r#"foo\
baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo<br />
baz</p>
"###.trim(), "Render mismatch for example 637");
    }
    #[test]
    fn render_example_638() {
        let ast = parse_phrases(r#"foo       
baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo<br />
baz</p>
"###.trim(), "Render mismatch for example 638");
    }
    #[test]
    fn render_example_639() {
        let ast = parse_phrases(r#"foo  
     bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo<br />
bar</p>
"###.trim(), "Render mismatch for example 639");
    }
    #[test]
    fn render_example_640() {
        let ast = parse_phrases(r#"foo\
     bar
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo<br />
bar</p>
"###.trim(), "Render mismatch for example 640");
    }
    #[test]
    fn render_example_641() {
        let ast = parse_phrases(r#"*foo  
bar*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo<br />
bar</em></p>
"###.trim(), "Render mismatch for example 641");
    }
    #[test]
    fn render_example_642() {
        let ast = parse_phrases(r#"*foo\
bar*
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><em>foo<br />
bar</em></p>
"###.trim(), "Render mismatch for example 642");
    }
    #[test]
    fn render_example_643() {
        let ast = parse_phrases(r#"`code  
span`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>code   span</code></p>
"###.trim(), "Render mismatch for example 643");
    }
    #[test]
    fn render_example_644() {
        let ast = parse_phrases(r#"`code\
span`
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><code>code\ span</code></p>
"###.trim(), "Render mismatch for example 644");
    }
    #[test]
    fn render_example_645() {
        let ast = parse_phrases(r#"<a href=\"foo  
bar\">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="foo  
bar"></p>
"###.trim(), "Render mismatch for example 645");
    }
    #[test]
    fn render_example_646() {
        let ast = parse_phrases(r#"<a href=\"foo\
bar\">
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p><a href="foo\
bar"></p>
"###.trim(), "Render mismatch for example 646");
    }
    #[test]
    fn render_example_647() {
        let ast = parse_phrases(r#"foo\
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo\</p>
"###.trim(), "Render mismatch for example 647");
    }
    #[test]
    fn render_example_648() {
        let ast = parse_phrases(r#"foo  
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo</p>
"###.trim(), "Render mismatch for example 648");
    }
    #[test]
    fn render_example_649() {
        let ast = parse_phrases(r#"### foo\
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h3>foo\</h3>
"###.trim(), "Render mismatch for example 649");
    }
    #[test]
    fn render_example_650() {
        let ast = parse_phrases(r#"### foo  
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<h3>foo</h3>
"###.trim(), "Render mismatch for example 650");
    }
    #[test]
    fn render_example_651() {
        let ast = parse_phrases(r#"foo
baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo
baz</p>
"###.trim(), "Render mismatch for example 651");
    }
    #[test]
    fn render_example_652() {
        let ast = parse_phrases(r#"foo 
 baz
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>foo
baz</p>
"###.trim(), "Render mismatch for example 652");
    }
    #[test]
    fn render_example_653() {
        let ast = parse_phrases(r#"hello $.;'there
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>hello $.;'there</p>
"###.trim(), "Render mismatch for example 653");
    }
    #[test]
    fn render_example_654() {
        let ast = parse_phrases(r#"Foo χρῆν
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Foo χρῆν</p>
"###.trim(), "Render mismatch for example 654");
    }
    #[test]
    fn render_example_655() {
        let ast = parse_phrases(r#"Multiple     spaces
"#);
        let inlines: Vec<(marco::logic::ast::inlines::Inline, marco::logic::core::event_types::SourcePos)> = ast.iter().map(|n| HtmlRenderer::inline_node_to_inline(n)).collect();
        let rendered = HtmlRenderer::render_inlines(&inlines);
        assert_eq!(rendered.trim(), r###"<p>Multiple     spaces</p>
"###.trim(), "Render mismatch for example 655");
    }
}
